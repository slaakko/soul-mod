<parserFile>
 <grammar name="LexerFileParser">
  <rule first="{#, #, ID, LBRACKET, SEMICOLON, ACTIONS, VARIABLES, RBRACE, RULES, LBRACE}" name="LexerFile">
   <action first="{#, #, ID, LBRACKET, SEMICOLON, ACTIONS, VARIABLES, RBRACE, RULES, LBRACE}">
    <group first="{#, #, ID, LBRACKET, SEMICOLON, ACTIONS, VARIABLES, RBRACE, RULES, LBRACE}">
     <sequence first="{#, #, ID, LBRACKET, SEMICOLON, ACTIONS, VARIABLES, RBRACE, RULES, LBRACE}">
      <sequence first="{#, #, ID, LBRACKET, SEMICOLON, ACTIONS, VARIABLES, RULES, LBRACE}">
       <sequence first="{#, #, ID, LBRACKET, SEMICOLON, LBRACE}">
        <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
         <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
          <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
           <sequence first="{#, ID}">
            <action first="{#}">
             <empty first="{#}"/>
            </action>
            <action first="{ID}">
             <nonterminal first="{ID}" ruleName="ExportModule"/>
            </action>
           </sequence>
           <nonterminal first="{#, #, ID, LBRACKET, SEMICOLON}" ruleName="Imports"/>
          </sequence>
          <nonterminal first="{ID}" ruleName="LexerKeyword"/>
         </sequence>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, ACTIONS, VARIABLES, RULES}">
        <group first="{ACTIONS, VARIABLES, RULES}">
         <nonterminal first="{ACTIONS, VARIABLES, RULES}" ruleName="LexerContent"/>
        </group>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, ID, LBRACKET, SEMICOLON}" name="Imports">
   <kleene first="{#, #, ID, LBRACKET, SEMICOLON}">
    <group first="{#, ID, LBRACKET, SEMICOLON}">
     <action first="{#, ID, LBRACKET, SEMICOLON}">
      <nonterminal first="{#, ID, LBRACKET, SEMICOLON}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{ACTIONS, VARIABLES, RULES}" name="LexerContent">
   <group first="{ACTIONS, VARIABLES, RULES}">
    <choice first="{ACTIONS, VARIABLES, RULES}">
     <choice first="{VARIABLES, RULES}">
      <nonterminal first="{RULES}" ruleName="Rules"/>
      <nonterminal first="{VARIABLES}" ruleName="Variables"/>
     </choice>
     <nonterminal first="{ACTIONS}" ruleName="Actions"/>
    </choice>
   </group>
  </rule>
  <rule first="{RULES}" name="Rules">
   <group first="{RULES}">
    <sequence first="{RULES}">
     <sequence first="{RULES}">
      <sequence first="{RULES}">
       <token first="{RULES}" name="RULES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STRING_LITERAL}">
       <group first="{STRING_LITERAL}">
        <action first="{STRING_LITERAL}">
         <nonterminal first="{STRING_LITERAL}" ruleName="Rule"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{STRING_LITERAL}" name="Rule">
   <action first="{STRING_LITERAL}">
    <group first="{STRING_LITERAL}">
     <sequence first="{STRING_LITERAL}">
      <sequence first="{STRING_LITERAL}">
       <action first="{STRING_LITERAL}">
        <nonterminal first="{STRING_LITERAL}" ruleName="ExprString"/>
       </action>
       <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
      </sequence>
      <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{VARIABLES}" name="Variables">
   <group first="{VARIABLES}">
    <sequence first="{VARIABLES}">
     <sequence first="{VARIABLES}">
      <sequence first="{VARIABLES}">
       <token first="{VARIABLES}" name="VARIABLES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, #, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
       <group first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
        <action first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
         <nonterminal first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}" ruleName="Variable"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}" name="Variable">
   <action first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
    <group first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
     <sequence first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
      <sequence first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}">
       <nonterminal first="{#, COLON_COLON, LONG, WCHART, SHORT, BOOL, SIGNED, FLOAT, VOID, ID, INT, CHAR16T, CHAR32T, VOLATILE, CONST, CHAR8T, CHAR, UNSIGNED, DOUBLE, #}" ruleName="TypeId"/>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ACTIONS}" name="Actions">
   <group first="{ACTIONS}">
    <sequence first="{ACTIONS}">
     <sequence first="{ACTIONS}">
      <sequence first="{ACTIONS}">
       <token first="{ACTIONS}" name="ACTIONS"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, ASSIGN, DOLLAR, LBRACE}">
       <group first="{#, ASSIGN, DOLLAR, LBRACE}">
        <sequence first="{#, ASSIGN, DOLLAR, LBRACE}">
         <sequence first="{#, ASSIGN, DOLLAR}">
          <action first="{#, DOLLAR}">
           <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
          </action>
          <token first="{ASSIGN}" name="ASSIGN"/>
         </sequence>
         <action first="{LBRACE}">
          <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, DOLLAR}" name="Action">
   <choice first="{#, DOLLAR}">
    <sequence first="{DOLLAR}">
     <sequence first="{DOLLAR}">
      <sequence first="{DOLLAR}">
       <token first="{DOLLAR}" name="DOLLAR"/>
       <token first="{LPAREN}" name="LPAREN"/>
      </sequence>
      <action first="{INTEGER_LITERAL}">
       <token first="{INTEGER_LITERAL}" name="INTEGER_LITERAL"/>
      </action>
     </sequence>
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </sequence>
    <action first="{#}">
     <empty first="{#}"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
