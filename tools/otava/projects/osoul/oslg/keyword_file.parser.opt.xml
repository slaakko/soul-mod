<parserFile>
 <grammar name="KeywordFileParser">
  <rule first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE, LPAREN, RBRACE}" name="KeywordFile">
   <action first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE, LPAREN, RBRACE}">
    <group first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE, LPAREN, RBRACE}">
     <sequence first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE, LPAREN, RBRACE}">
      <sequence first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE, LPAREN}">
       <sequence first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID, LBRACE}">
        <sequence first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID}">
         <sequence first="{#, #, KEYWORDS, LBRACKET, SEMICOLON, ID}">
          <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
           <action first="{#}">
            <empty first="{#}"/>
           </action>
           <nonterminal first="{#, #, LBRACKET, SEMICOLON, ID}" ruleName="Imports"/>
          </sequence>
          <token first="{KEYWORDS}" name="KEYWORDS"/>
         </sequence>
         <action first="{ID}">
          <nonterminal first="{ID}" ruleName="QualifiedId"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <list first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <nonterminal first="{LPAREN}" ruleName="Keyword"/>
           </action>
           <kleene first="{#, COMMA}">
            <sequence first="{COMMA}">
             <token first="{COMMA}" name="COMMA"/>
             <action first="{LPAREN}">
              <nonterminal first="{LPAREN}" ruleName="Keyword"/>
             </action>
            </sequence>
           </kleene>
          </sequence>
         </list>
        </group>
       </optional>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, LBRACKET, SEMICOLON, ID}" name="Imports">
   <kleene first="{#, #, LBRACKET, SEMICOLON, ID}">
    <group first="{#, LBRACKET, SEMICOLON, ID}">
     <action first="{#, LBRACKET, SEMICOLON, ID}">
      <nonterminal first="{#, LBRACKET, SEMICOLON, ID}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{LPAREN}" name="Keyword">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{STRING_LITERAL}">
          <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
         </action>
        </sequence>
        <token first="{COMMA}" name="COMMA"/>
       </sequence>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
