<parserFile>
 <grammar name="LexerFileParser">
  <rule first="{#, #, ACTIONS, VARIABLES, RULES, LBRACKET, SEMICOLON, ID, LBRACE, RBRACE}" name="LexerFile">
   <action first="{#, #, ACTIONS, VARIABLES, RULES, LBRACKET, SEMICOLON, ID, LBRACE, RBRACE}">
    <group first="{#, #, ACTIONS, VARIABLES, RULES, LBRACKET, SEMICOLON, ID, LBRACE, RBRACE}">
     <sequence first="{#, #, ACTIONS, VARIABLES, RULES, LBRACKET, SEMICOLON, ID, LBRACE, RBRACE}">
      <sequence first="{#, #, ACTIONS, VARIABLES, RULES, LBRACKET, SEMICOLON, ID, LBRACE}">
       <sequence first="{#, #, LBRACKET, SEMICOLON, ID, LBRACE}">
        <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
         <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
          <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
           <sequence first="{#, ID}">
            <action first="{#}">
             <empty first="{#}"/>
            </action>
            <action first="{ID}">
             <nonterminal first="{ID}" ruleName="ExportModule"/>
            </action>
           </sequence>
           <nonterminal first="{#, #, LBRACKET, SEMICOLON, ID}" ruleName="Imports"/>
          </sequence>
          <nonterminal first="{ID}" ruleName="LexerKeyword"/>
         </sequence>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, ACTIONS, VARIABLES, RULES}">
        <group first="{ACTIONS, VARIABLES, RULES}">
         <nonterminal first="{ACTIONS, VARIABLES, RULES}" ruleName="LexerContent"/>
        </group>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, LBRACKET, SEMICOLON, ID}" name="Imports">
   <kleene first="{#, #, LBRACKET, SEMICOLON, ID}">
    <group first="{#, LBRACKET, SEMICOLON, ID}">
     <action first="{#, LBRACKET, SEMICOLON, ID}">
      <nonterminal first="{#, LBRACKET, SEMICOLON, ID}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{ACTIONS, VARIABLES, RULES}" name="LexerContent">
   <group first="{ACTIONS, VARIABLES, RULES}">
    <switch first="{ACTIONS, VARIABLES, RULES}">
     <case first="{RULES}">
      <nonterminal first="{RULES}" ruleName="Rules"/>
     </case>
     <case first="{VARIABLES}">
      <nonterminal first="{VARIABLES}" ruleName="Variables"/>
     </case>
     <case first="{ACTIONS}">
      <nonterminal first="{ACTIONS}" ruleName="Actions"/>
     </case>
    </switch>
   </group>
  </rule>
  <rule first="{RULES}" name="Rules">
   <group first="{RULES}">
    <sequence first="{RULES}">
     <sequence first="{RULES}">
      <sequence first="{RULES}">
       <token first="{RULES}" name="RULES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STRING_LITERAL}">
       <group first="{STRING_LITERAL}">
        <action first="{STRING_LITERAL}">
         <nonterminal first="{STRING_LITERAL}" ruleName="Rule"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{STRING_LITERAL}" name="Rule">
   <action first="{STRING_LITERAL}">
    <group first="{STRING_LITERAL}">
     <sequence first="{STRING_LITERAL}">
      <sequence first="{STRING_LITERAL}">
       <action first="{STRING_LITERAL}">
        <nonterminal first="{STRING_LITERAL}" ruleName="ExprString"/>
       </action>
       <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
      </sequence>
      <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{VARIABLES}" name="Variables">
   <group first="{VARIABLES}">
    <sequence first="{VARIABLES}">
     <sequence first="{VARIABLES}">
      <sequence first="{VARIABLES}">
       <token first="{VARIABLES}" name="VARIABLES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, #, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
       <group first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
        <action first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
         <nonterminal first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}" ruleName="Variable"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}" name="Variable">
   <action first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
    <group first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
     <sequence first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
      <sequence first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}">
       <nonterminal first="{#, #, ID, COLON_COLON, CHAR8T, BOOL, VOLATILE, VOID, FLOAT, INT, LONG, CHAR, CHAR16T, UNSIGNED, WCHART, CONST, CHAR32T, SHORT, SIGNED, DOUBLE}" ruleName="TypeId"/>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ACTIONS}" name="Actions">
   <group first="{ACTIONS}">
    <sequence first="{ACTIONS}">
     <sequence first="{ACTIONS}">
      <sequence first="{ACTIONS}">
       <token first="{ACTIONS}" name="ACTIONS"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, DOLLAR, LBRACE, ASSIGN}">
       <group first="{#, DOLLAR, LBRACE, ASSIGN}">
        <sequence first="{#, DOLLAR, LBRACE, ASSIGN}">
         <sequence first="{#, DOLLAR, ASSIGN}">
          <action first="{#, DOLLAR}">
           <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
          </action>
          <token first="{ASSIGN}" name="ASSIGN"/>
         </sequence>
         <action first="{LBRACE}">
          <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, DOLLAR}" name="Action">
   <choice first="{#, DOLLAR}">
    <sequence first="{DOLLAR}">
     <sequence first="{DOLLAR}">
      <sequence first="{DOLLAR}">
       <token first="{DOLLAR}" name="DOLLAR"/>
       <token first="{LPAREN}" name="LPAREN"/>
      </sequence>
      <action first="{INTEGER_LITERAL}">
       <token first="{INTEGER_LITERAL}" name="INTEGER_LITERAL"/>
      </action>
     </sequence>
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </sequence>
    <action first="{#}">
     <empty first="{#}"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
