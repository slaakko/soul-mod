<parserFile>
 <grammar name="RexParser">
  <rule first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" name="RegularExpression">
   <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
     <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" name="Alternative">
   <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
     <sequence first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
      <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
       <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
          <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" name="Catenation">
   <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
     <sequence first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
      <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
       <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
       <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
        <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
         <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" name="Repetition">
   <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
     <sequence first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
      <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
       <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <choice first="{PLUS, STAR, QUEST}">
         <choice first="{PLUS, STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" name="Primary">
   <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
     <choice first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
      <choice first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, INVERSE}">
       <choice first="{LPAREN, ESCAPE, CHAR, MINUS, INVERSE}">
        <choice first="{LPAREN, ESCAPE, MINUS, INVERSE}">
         <choice first="{LPAREN, ESCAPE, INVERSE}">
          <choice first="{LPAREN, ESCAPE}">
           <sequence first="{LPAREN}">
            <sequence first="{LPAREN}">
             <token first="{LPAREN}" name="LPAREN"/>
             <action first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
              <expectation first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}">
               <nonterminal first="{LPAREN, ESCAPE, DOT, CHAR, MINUS, LBRACKET, INVERSE}" ruleName="Alternative"/>
              </expectation>
             </action>
            </sequence>
            <token first="{RPAREN}" name="RPAREN"/>
           </sequence>
           <action first="{ESCAPE}">
            <token first="{ESCAPE}" name="ESCAPE"/>
           </action>
          </choice>
          <action first="{INVERSE}">
           <token first="{INVERSE}" name="INVERSE"/>
          </action>
         </choice>
         <action first="{MINUS}">
          <token first="{MINUS}" name="MINUS"/>
         </action>
        </choice>
        <action first="{CHAR}">
         <token first="{CHAR}" name="CHAR"/>
        </action>
       </choice>
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </choice>
      <action first="{LBRACKET}">
       <nonterminal first="{LBRACKET}" ruleName="Class"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
        <sequence first="{#, LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
          <group first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
           <action first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
            <nonterminal first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}" name="Range">
   <action first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
    <group first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
     <sequence first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
      <nonterminal first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
          <nonterminal first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}" name="Char">
   <choice first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, CHAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
    <choice first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, ALT, QUEST, MINUS, LBRACKET, INVERSE}">
     <choice first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, ALT, QUEST, LBRACKET, INVERSE}">
      <choice first="{LPAREN, RPAREN, PLUS, ESCAPE, DOT, STAR, ALT, QUEST, LBRACKET}">
       <choice first="{LPAREN, RPAREN, PLUS, DOT, STAR, ALT, QUEST, LBRACKET}">
        <choice first="{LPAREN, RPAREN, PLUS, STAR, ALT, QUEST, LBRACKET}">
         <choice first="{LPAREN, RPAREN, PLUS, STAR, ALT, LBRACKET}">
          <choice first="{LPAREN, RPAREN, STAR, ALT, LBRACKET}">
           <choice first="{LPAREN, RPAREN, ALT, LBRACKET}">
            <choice first="{LPAREN, RPAREN, LBRACKET}">
             <choice first="{LPAREN, RPAREN}">
              <action first="{LPAREN}">
               <token first="{LPAREN}" name="LPAREN"/>
              </action>
              <action first="{RPAREN}">
               <token first="{RPAREN}" name="RPAREN"/>
              </action>
             </choice>
             <action first="{LBRACKET}">
              <token first="{LBRACKET}" name="LBRACKET"/>
             </action>
            </choice>
            <action first="{ALT}">
             <token first="{ALT}" name="ALT"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{INVERSE}">
       <token first="{INVERSE}" name="INVERSE"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHAR}">
     <token first="{CHAR}" name="CHAR"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
