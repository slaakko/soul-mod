<parserFile>
 <grammar name="RexParser">
  <rule first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" name="RegularExpression">
   <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
    <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
     <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" name="Alternative">
   <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
    <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
     <sequence first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
      <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
       <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
          <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" name="Catenation">
   <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
    <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
     <sequence first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
      <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
       <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
       <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
        <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
         <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" name="Repetition">
   <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
    <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
     <sequence first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
      <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
       <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <choice first="{PLUS, STAR, QUEST}">
         <choice first="{PLUS, STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" name="Primary">
   <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
    <group first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
     <choice first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
      <choice first="{CHAR, DOT, ESCAPE, MINUS, LPAREN, INVERSE}">
       <choice first="{CHAR, ESCAPE, MINUS, LPAREN, INVERSE}">
        <choice first="{ESCAPE, MINUS, LPAREN, INVERSE}">
         <choice first="{ESCAPE, LPAREN, INVERSE}">
          <choice first="{ESCAPE, LPAREN}">
           <sequence first="{LPAREN}">
            <sequence first="{LPAREN}">
             <token first="{LPAREN}" name="LPAREN"/>
             <action first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
              <expectation first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}">
               <nonterminal first="{CHAR, DOT, ESCAPE, MINUS, LBRACKET, LPAREN, INVERSE}" ruleName="Alternative"/>
              </expectation>
             </action>
            </sequence>
            <token first="{RPAREN}" name="RPAREN"/>
           </sequence>
           <action first="{ESCAPE}">
            <token first="{ESCAPE}" name="ESCAPE"/>
           </action>
          </choice>
          <action first="{INVERSE}">
           <token first="{INVERSE}" name="INVERSE"/>
          </action>
         </choice>
         <action first="{MINUS}">
          <token first="{MINUS}" name="MINUS"/>
         </action>
        </choice>
        <action first="{CHAR}">
         <token first="{CHAR}" name="CHAR"/>
        </action>
       </choice>
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </choice>
      <action first="{LBRACKET}">
       <nonterminal first="{LBRACKET}" ruleName="Class"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
        <sequence first="{#, CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
          <group first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
           <action first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
            <nonterminal first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}" name="Range">
   <action first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
    <group first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
     <sequence first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
      <nonterminal first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
          <nonterminal first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}" name="Char">
   <choice first="{CHAR, ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
    <choice first="{ALT, DOT, PLUS, ESCAPE, MINUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
     <choice first="{ALT, DOT, PLUS, ESCAPE, RPAREN, LBRACKET, LPAREN, STAR, QUEST, INVERSE}">
      <choice first="{ALT, DOT, PLUS, ESCAPE, RPAREN, LBRACKET, LPAREN, STAR, QUEST}">
       <choice first="{ALT, DOT, PLUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST}">
        <choice first="{ALT, PLUS, RPAREN, LBRACKET, LPAREN, STAR, QUEST}">
         <choice first="{ALT, PLUS, RPAREN, LBRACKET, LPAREN, STAR}">
          <choice first="{ALT, RPAREN, LBRACKET, LPAREN, STAR}">
           <choice first="{ALT, RPAREN, LBRACKET, LPAREN}">
            <choice first="{RPAREN, LBRACKET, LPAREN}">
             <choice first="{RPAREN, LPAREN}">
              <action first="{LPAREN}">
               <token first="{LPAREN}" name="LPAREN"/>
              </action>
              <action first="{RPAREN}">
               <token first="{RPAREN}" name="RPAREN"/>
              </action>
             </choice>
             <action first="{LBRACKET}">
              <token first="{LBRACKET}" name="LBRACKET"/>
             </action>
            </choice>
            <action first="{ALT}">
             <token first="{ALT}" name="ALT"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{INVERSE}">
       <token first="{INVERSE}" name="INVERSE"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHAR}">
     <token first="{CHAR}" name="CHAR"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
