<parserFile>
 <grammar name="RexParser">
  <rule first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" name="RegularExpression">
   <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
    <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
     <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" name="Alternative">
   <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
    <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
      <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
          <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" name="Catenation">
   <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
    <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
      <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
       <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
        <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
         <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" name="Repetition">
   <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
    <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
      <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <choice first="{STAR, PLUS, QUEST}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" name="Primary">
   <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
    <group first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
     <choice first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
      <choice first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LPAREN}">
       <choice first="{MINUS, CHAR, ESCAPE, INVERSE, LPAREN}">
        <choice first="{MINUS, ESCAPE, INVERSE, LPAREN}">
         <choice first="{ESCAPE, INVERSE, LPAREN}">
          <choice first="{ESCAPE, LPAREN}">
           <sequence first="{LPAREN}">
            <sequence first="{LPAREN}">
             <token first="{LPAREN}" name="LPAREN"/>
             <action first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
              <expectation first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}">
               <nonterminal first="{MINUS, CHAR, DOT, ESCAPE, INVERSE, LBRACKET, LPAREN}" ruleName="Alternative"/>
              </expectation>
             </action>
            </sequence>
            <token first="{RPAREN}" name="RPAREN"/>
           </sequence>
           <action first="{ESCAPE}">
            <token first="{ESCAPE}" name="ESCAPE"/>
           </action>
          </choice>
          <action first="{INVERSE}">
           <token first="{INVERSE}" name="INVERSE"/>
          </action>
         </choice>
         <action first="{MINUS}">
          <token first="{MINUS}" name="MINUS"/>
         </action>
        </choice>
        <action first="{CHAR}">
         <token first="{CHAR}" name="CHAR"/>
        </action>
       </choice>
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </choice>
      <action first="{LBRACKET}">
       <nonterminal first="{LBRACKET}" ruleName="Class"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
        <sequence first="{#, MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
          <group first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
           <action first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
            <nonterminal first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}" name="Range">
   <action first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
    <group first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
     <sequence first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
      <nonterminal first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
          <nonterminal first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}" name="Char">
   <choice first="{MINUS, CHAR, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
    <choice first="{MINUS, ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
     <choice first="{ALT, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
      <choice first="{ALT, DOT, ESCAPE, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
       <choice first="{ALT, DOT, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
        <choice first="{ALT, RPAREN, LBRACKET, LPAREN, STAR, PLUS, QUEST}">
         <choice first="{ALT, RPAREN, LBRACKET, LPAREN, STAR, PLUS}">
          <choice first="{ALT, RPAREN, LBRACKET, LPAREN, STAR}">
           <choice first="{ALT, RPAREN, LBRACKET, LPAREN}">
            <choice first="{RPAREN, LBRACKET, LPAREN}">
             <choice first="{RPAREN, LPAREN}">
              <action first="{LPAREN}">
               <token first="{LPAREN}" name="LPAREN"/>
              </action>
              <action first="{RPAREN}">
               <token first="{RPAREN}" name="RPAREN"/>
              </action>
             </choice>
             <action first="{LBRACKET}">
              <token first="{LBRACKET}" name="LBRACKET"/>
             </action>
            </choice>
            <action first="{ALT}">
             <token first="{ALT}" name="ALT"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{INVERSE}">
       <token first="{INVERSE}" name="INVERSE"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHAR}">
     <token first="{CHAR}" name="CHAR"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
