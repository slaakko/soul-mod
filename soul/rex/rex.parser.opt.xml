<parserFile>
 <grammar name="RexParser">
  <rule first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" name="RegularExpression">
   <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
    <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
     <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" name="Alternative">
   <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
    <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
     <sequence first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
      <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
          <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" name="Catenation">
   <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
    <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
     <sequence first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
      <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
       <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
        <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
         <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" name="Repetition">
   <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
    <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
     <sequence first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
      <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, QUEST, PLUS}">
       <group first="{STAR, QUEST, PLUS}">
        <switch first="{STAR, QUEST, PLUS}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" name="Primary">
   <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
    <group first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
     <switch first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
          <expectation first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}">
           <nonterminal first="{LPAREN, LBRACKET, CHAR, INVERSE, DOT, MINUS, ESCAPE}" ruleName="Alternative"/>
          </expectation>
         </action>
        </sequence>
        <token first="{RPAREN}" name="RPAREN"/>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{INVERSE}">
       <action first="{INVERSE}">
        <token first="{INVERSE}" name="INVERSE"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHAR}">
       <action first="{CHAR}">
        <token first="{CHAR}" name="CHAR"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
        <sequence first="{#, ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
          <group first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
           <action first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
            <nonterminal first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}" name="Range">
   <action first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
    <group first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
     <sequence first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
      <nonterminal first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
          <nonterminal first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}" name="Char">
   <switch first="{ALT, STAR, LPAREN, RPAREN, LBRACKET, CHAR, QUEST, INVERSE, DOT, MINUS, ESCAPE, PLUS}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{ALT}">
     <action first="{ALT}">
      <token first="{ALT}" name="ALT"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{INVERSE}">
     <action first="{INVERSE}">
      <token first="{INVERSE}" name="INVERSE"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHAR}">
     <action first="{CHAR}">
      <token first="{CHAR}" name="CHAR"/>
     </action>
    </case>
   </switch>
  </rule>
 </grammar>
</parserFile>
