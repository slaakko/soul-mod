<parserFile>
 <grammar name="RexParser">
  <rule first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" name="RegularExpression">
   <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
     <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" name="Alternative">
   <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
     <sequence first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
      <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
       <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
          <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" name="Catenation">
   <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
     <sequence first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
      <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
       <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
       <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
        <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
         <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" name="Repetition">
   <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
     <sequence first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
      <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
       <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <switch first="{PLUS, STAR, QUEST}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" name="Primary">
   <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
     <switch first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
          <expectation first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}">
           <nonterminal first="{CHAR, MINUS, INVERSE, LBRACKET, LPAREN, ESCAPE, DOT}" ruleName="Alternative"/>
          </expectation>
         </action>
        </sequence>
        <token first="{RPAREN}" name="RPAREN"/>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{INVERSE}">
       <action first="{INVERSE}">
        <token first="{INVERSE}" name="INVERSE"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHAR}">
       <action first="{CHAR}">
        <token first="{CHAR}" name="CHAR"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
        <sequence first="{#, CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
          <group first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
           <action first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
            <nonterminal first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}" name="Range">
   <action first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
    <group first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
     <sequence first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
      <nonterminal first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
          <nonterminal first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}" name="Char">
   <switch first="{CHAR, PLUS, MINUS, RPAREN, STAR, QUEST, INVERSE, LBRACKET, ALT, LPAREN, ESCAPE, DOT}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{ALT}">
     <action first="{ALT}">
      <token first="{ALT}" name="ALT"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{INVERSE}">
     <action first="{INVERSE}">
      <token first="{INVERSE}" name="INVERSE"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHAR}">
     <action first="{CHAR}">
      <token first="{CHAR}" name="CHAR"/>
     </action>
    </case>
   </switch>
  </rule>
 </grammar>
</parserFile>
