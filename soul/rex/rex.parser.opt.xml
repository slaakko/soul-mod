<parserFile>
 <grammar name="RexParser">
  <rule first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" name="RegularExpression">
   <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
    <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
     <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" name="Alternative">
   <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
    <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
     <sequence first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
      <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
       <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
          <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" name="Catenation">
   <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
    <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
     <sequence first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
      <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
       <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
       <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
        <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
         <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" name="Repetition">
   <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
    <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
     <sequence first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
      <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
       <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, QUEST, STAR}">
       <group first="{PLUS, QUEST, STAR}">
        <switch first="{PLUS, QUEST, STAR}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" name="Primary">
   <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
    <group first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
     <switch first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
          <expectation first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}">
           <nonterminal first="{LPAREN, INVERSE, ESCAPE, MINUS, DOT, CHAR, LBRACKET}" ruleName="Alternative"/>
          </expectation>
         </action>
        </sequence>
        <token first="{RPAREN}" name="RPAREN"/>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{INVERSE}">
       <action first="{INVERSE}">
        <token first="{INVERSE}" name="INVERSE"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHAR}">
       <action first="{CHAR}">
        <token first="{CHAR}" name="CHAR"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
        <sequence first="{#, PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
          <group first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
           <action first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
            <nonterminal first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}" name="Range">
   <action first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
    <group first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
     <sequence first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
      <nonterminal first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
          <nonterminal first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}" name="Char">
   <switch first="{PLUS, LPAREN, INVERSE, ESCAPE, RPAREN, ALT, MINUS, DOT, CHAR, LBRACKET, QUEST, STAR}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{ALT}">
     <action first="{ALT}">
      <token first="{ALT}" name="ALT"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{INVERSE}">
     <action first="{INVERSE}">
      <token first="{INVERSE}" name="INVERSE"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHAR}">
     <action first="{CHAR}">
      <token first="{CHAR}" name="CHAR"/>
     </action>
    </case>
   </switch>
  </rule>
 </grammar>
</parserFile>
