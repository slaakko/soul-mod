<parserFile>
 <grammar name="SpgFileParser">
  <rule first="{PROJECT}" name="SpgFile">
   <action first="{PROJECT}">
    <group first="{PROJECT}">
     <sequence first="{PROJECT}">
      <sequence first="{PROJECT}">
       <sequence first="{PROJECT}">
        <token first="{PROJECT}" name="PROJECT"/>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="QualifiedId"/>
        </action>
       </sequence>
       <token first="{SEMICOLON}" name="SEMICOLON"/>
      </sequence>
      <kleene first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}">
       <group first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}">
        <action first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}">
         <nonterminal first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}" ruleName="SpgFileDeclaration"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}" name="SpgFileDeclaration">
   <choice first="{#, EXTERN, SEMICOLON, FILEPATH, ID, TOKENS}">
    <action first="{#, EXTERN, SEMICOLON, FILEPATH, ID}">
     <nonterminal first="{#, EXTERN, SEMICOLON, FILEPATH, ID}" ruleName="ParserFileDeclaration"/>
    </action>
    <action first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}">
     <nonterminal first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}" ruleName="TokenFileDeclaration"/>
    </action>
   </choice>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, FILEPATH, ID}" name="ParserFileDeclaration">
   <action first="{#, EXTERN, SEMICOLON, FILEPATH, ID}">
    <group first="{#, EXTERN, SEMICOLON, FILEPATH, ID}">
     <sequence first="{#, EXTERN, SEMICOLON, FILEPATH, ID}">
      <sequence first="{#, EXTERN, FILEPATH, ID}">
       <sequence first="{#, EXTERN, ID}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <nonterminal first="{ID}" ruleName="ParserKeyword"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}" name="TokenFileDeclaration">
   <action first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}">
    <group first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}">
     <sequence first="{#, EXTERN, SEMICOLON, FILEPATH, TOKENS}">
      <sequence first="{#, EXTERN, FILEPATH, TOKENS}">
       <sequence first="{#, EXTERN, TOKENS}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <token first="{TOKENS}" name="TOKENS"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
