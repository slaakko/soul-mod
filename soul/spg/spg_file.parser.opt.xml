<parserFile>
 <grammar name="SpgFileParser">
  <rule first="{PROJECT}" name="SpgFile">
   <action first="{PROJECT}">
    <group first="{PROJECT}">
     <sequence first="{PROJECT}">
      <sequence first="{PROJECT}">
       <sequence first="{PROJECT}">
        <token first="{PROJECT}" name="PROJECT"/>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="QualifiedId"/>
        </action>
       </sequence>
       <token first="{SEMICOLON}" name="SEMICOLON"/>
      </sequence>
      <kleene first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
       <group first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
        <action first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
         <nonterminal first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}" ruleName="SpgFileDeclaration"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}" name="SpgFileDeclaration">
   <choice first="{#, ID, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
    <action first="{#, ID, EXTERN, FILEPATH, SEMICOLON}">
     <nonterminal first="{#, ID, EXTERN, FILEPATH, SEMICOLON}" ruleName="ParserFileDeclaration"/>
    </action>
    <action first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
     <nonterminal first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}" ruleName="TokenFileDeclaration"/>
    </action>
   </choice>
  </rule>
  <rule first="{#, ID, EXTERN, FILEPATH, SEMICOLON}" name="ParserFileDeclaration">
   <action first="{#, ID, EXTERN, FILEPATH, SEMICOLON}">
    <group first="{#, ID, EXTERN, FILEPATH, SEMICOLON}">
     <sequence first="{#, ID, EXTERN, FILEPATH, SEMICOLON}">
      <sequence first="{#, ID, EXTERN, FILEPATH}">
       <sequence first="{#, ID, EXTERN}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <nonterminal first="{ID}" ruleName="ParserKeyword"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}" name="TokenFileDeclaration">
   <action first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
    <group first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
     <sequence first="{#, EXTERN, FILEPATH, TOKENS, SEMICOLON}">
      <sequence first="{#, EXTERN, FILEPATH, TOKENS}">
       <sequence first="{#, EXTERN, TOKENS}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <token first="{TOKENS}" name="TOKENS"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
