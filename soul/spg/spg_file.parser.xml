<parserFile>
 <grammar name="SpgFileParser">
  <rule first="{PROJECT}" name="SpgFile">
   <action first="{PROJECT}">
    <group first="{PROJECT}">
     <sequence first="{PROJECT}">
      <sequence first="{PROJECT}">
       <sequence first="{PROJECT}">
        <token first="{PROJECT}" name="PROJECT"/>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="QualifiedId"/>
        </action>
       </sequence>
       <token first="{SEMICOLON}" name="SEMICOLON"/>
      </sequence>
      <kleene first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}">
       <group first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}">
        <action first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}">
         <nonterminal first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}" ruleName="SpgFileDeclaration"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}" name="SpgFileDeclaration">
   <choice first="{#, EXTERN, SEMICOLON, ID, TOKENS, FILEPATH}">
    <action first="{#, EXTERN, SEMICOLON, ID, FILEPATH}">
     <nonterminal first="{#, EXTERN, SEMICOLON, ID, FILEPATH}" ruleName="ParserFileDeclaration"/>
    </action>
    <action first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}">
     <nonterminal first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}" ruleName="TokenFileDeclaration"/>
    </action>
   </choice>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, ID, FILEPATH}" name="ParserFileDeclaration">
   <action first="{#, EXTERN, SEMICOLON, ID, FILEPATH}">
    <group first="{#, EXTERN, SEMICOLON, ID, FILEPATH}">
     <sequence first="{#, EXTERN, SEMICOLON, ID, FILEPATH}">
      <sequence first="{#, EXTERN, ID, FILEPATH}">
       <sequence first="{#, EXTERN, ID}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <nonterminal first="{ID}" ruleName="ParserKeyword"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}" name="TokenFileDeclaration">
   <action first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}">
    <group first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}">
     <sequence first="{#, EXTERN, SEMICOLON, TOKENS, FILEPATH}">
      <sequence first="{#, EXTERN, TOKENS, FILEPATH}">
       <sequence first="{#, EXTERN, TOKENS}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <token first="{TOKENS}" name="TOKENS"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
