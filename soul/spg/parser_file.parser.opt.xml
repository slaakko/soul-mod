<parserFile>
 <grammar name="ParserFileParser">
  <rule first="{#, #, SEMICOLON, LBRACKET, ID}" name="ParserFile">
   <action first="{#, #, SEMICOLON, LBRACKET, ID}">
    <group first="{#, #, SEMICOLON, LBRACKET, ID}">
     <sequence first="{#, #, SEMICOLON, LBRACKET, ID}">
      <sequence first="{#, #, SEMICOLON, LBRACKET, ID}">
       <sequence first="{#, ID}">
        <action first="{#}">
         <empty first="{#}"/>
        </action>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="ExportModule"/>
        </action>
       </sequence>
       <kleene first="{#, #, SEMICOLON, LBRACKET, ID}">
        <group first="{#, SEMICOLON, LBRACKET, ID}">
         <action first="{#, SEMICOLON, LBRACKET, ID}">
          <nonterminal first="{#, SEMICOLON, LBRACKET, ID}" ruleName="Import"/>
         </action>
        </group>
       </kleene>
      </sequence>
      <kleene first="{#, ID}">
       <group first="{ID}">
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="Parser"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="Parser">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <nonterminal first="{ID}" ruleName="ParserKeyword"/>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, USING, MAIN, ID}">
        <nonterminal first="{USING, MAIN, ID}" ruleName="ParserStatement"/>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{USING, MAIN, ID}" name="ParserStatement">
   <choice first="{USING, MAIN, ID}">
    <switch first="{USING, MAIN, ID}">
     <case first="{ID}">
      <nonterminal first="{ID}" ruleName="LexerStatement"/>
     </case>
     <case first="{MAIN}">
      <nonterminal first="{MAIN}" ruleName="MainStatement"/>
     </case>
     <case first="{USING}">
      <nonterminal first="{USING}" ruleName="UsingStatement"/>
     </case>
    </switch>
    <nonterminal first="{ID}" ruleName="RuleStatement"/>
   </choice>
  </rule>
  <rule first="{ID}" name="LexerStatement">
   <sequence first="{ID}">
    <sequence first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerKeyword"/>
     <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}" ruleName="TypeId"/>
    </sequence>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{MAIN}" name="MainStatement">
   <sequence first="{MAIN}">
    <token first="{MAIN}" name="MAIN"/>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{USING}" name="UsingStatement">
   <action first="{USING}">
    <group first="{USING}">
     <sequence first="{USING}">
      <sequence first="{USING}">
       <token first="{USING}" name="USING"/>
       <nonterminal first="{ID}" ruleName="QualifiedId"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleStatement">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <sequence first="{ID}">
          <action first="{ID}">
           <token first="{ID}" name="ID"/>
          </action>
          <optional first="{#, LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="ParametersAndVariables"/>
          </optional>
         </sequence>
         <optional first="{#, COLON}">
          <nonterminal first="{COLON}" ruleName="ReturnType"/>
         </optional>
        </sequence>
        <token first="{PRODUCES}" name="PRODUCES"/>
       </sequence>
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="RuleBody"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN}" name="ParametersAndVariables">
   <sequence first="{LPAREN}">
    <sequence first="{LPAREN}">
     <token first="{LPAREN}" name="LPAREN"/>
     <optional first="{#, #, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
      <group first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
       <list first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
        <sequence first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
         <action first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
          <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}" ruleName="ParamOrVariable"/>
         </action>
         <kleene first="{#, COMMA}">
          <sequence first="{COMMA}">
           <token first="{COMMA}" name="COMMA"/>
           <action first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
            <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}" ruleName="ParamOrVariable"/>
           </action>
          </sequence>
         </kleene>
        </sequence>
       </list>
      </group>
     </optional>
    </sequence>
    <token first="{RPAREN}" name="RPAREN"/>
   </sequence>
  </rule>
  <rule first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}" name="ParamOrVariable">
   <switch first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, VAR, ID}">
    <case first="{VAR}">
     <sequence first="{VAR}">
      <sequence first="{VAR}">
       <token first="{VAR}" name="VAR"/>
       <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}" ruleName="TypeId"/>
      </sequence>
      <action first="{#, #, STAR, OPERATOR, COLON_COLON, AMP, ID}">
       <nonterminal first="{#, #, STAR, OPERATOR, COLON_COLON, AMP, ID}" ruleName="Declarator"/>
      </action>
     </sequence>
    </case>
    <case first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}">
     <sequence first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}">
      <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}" ruleName="TypeId"/>
      <action first="{#, #, STAR, OPERATOR, COLON_COLON, AMP, ID}">
       <nonterminal first="{#, #, STAR, OPERATOR, COLON_COLON, AMP, ID}" ruleName="Declarator"/>
      </action>
     </sequence>
    </case>
   </switch>
  </rule>
  <rule first="{COLON}" name="ReturnType">
   <sequence first="{COLON}">
    <token first="{COLON}" name="COLON"/>
    <action first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}">
     <nonterminal first="{#, #, CONST, FLOAT, VOID, CHAR16T, VOLATILE, BOOL, CHAR, INT, LONG, SIGNED, CHAR8T, CHAR32T, COLON_COLON, UNSIGNED, WCHART, DOUBLE, SHORT, ID}" ruleName="TypeId"/>
    </action>
   </sequence>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="RuleBody">
   <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Choice"/>
   </action>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Choice">
   <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Sequence"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
          <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Sequence"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Sequence">
   <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Difference"/>
      </action>
      <kleene first="{#, LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <group first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
        <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
         <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Difference"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Difference">
   <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="List"/>
      </action>
      <kleene first="{#, MINUS}">
       <group first="{MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
          <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="List"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="List">
   <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Prefix"/>
      </action>
      <optional first="{#, REM}">
       <group first="{REM}">
        <sequence first="{REM}">
         <token first="{REM}" name="REM"/>
         <action first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
          <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Prefix"/>
         </action>
        </sequence>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Prefix">
   <switch first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <case first="{AMP}">
     <sequence first="{AMP}">
      <token first="{AMP}" name="AMP"/>
      <action first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Postfix"/>
      </action>
     </sequence>
    </case>
    <case first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <action first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <nonterminal first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Postfix"/>
     </action>
    </case>
   </switch>
  </rule>
  <rule first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Postfix">
   <action first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <action first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <nonterminal first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <switch first="{STAR, PLUS, QUEST}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Primary">
   <action first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <group first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <sequence first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
      <sequence first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
       <group first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
        <switch first="{LPAREN, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
         <case first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
          <choice first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
           <action first="{ID}">
            <nonterminal first="{ID}" ruleName="RuleCall"/>
           </action>
           <action first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
            <nonterminal first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Primitive"/>
           </action>
          </choice>
         </case>
         <case first="{LPAREN}">
          <action first="{LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="Grouping"/>
          </action>
         </case>
        </switch>
       </group>
       <optional first="{#, EXCLAMATION}">
        <group first="{EXCLAMATION}">
         <action first="{EXCLAMATION}">
          <token first="{EXCLAMATION}" name="EXCLAMATION"/>
         </action>
        </group>
       </optional>
      </sequence>
      <optional first="{#, LBRACE}">
       <group first="{LBRACE}">
        <action first="{LBRACE}">
         <group first="{LBRACE}">
          <sequence first="{LBRACE}">
           <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
           <optional first="{#, DIV}">
            <group first="{DIV}">
             <sequence first="{DIV}">
              <token first="{DIV}" name="DIV"/>
              <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
             </sequence>
            </group>
           </optional>
          </sequence>
         </group>
        </action>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleCall">
   <group first="{ID}">
    <sequence first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <action first="{ID}">
        <nonterminal first="{ID}" ruleName="Nonterminal"/>
       </action>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <sequence first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <token first="{LPAREN}" name="LPAREN"/>
           </action>
           <nonterminal first="{#, #, DISJUNCTION, AMP_AMP, SHIFT_RIGHT_ASSIGN, CONST, FLOAT, STAR, SHIFT_LEFT_ASSIGN, MUL_ASSIGN, MINUS, OR_ASSIGN, VOID, SHIFT_LEFT, CHAR16T, MINUS_MINUS, VOLATILE, BOOL, SIZEOF, OPERATOR, CHAR, INT, LEQ, STATIC_CAST, PLUS_PLUS, PLUS_ASSIGN, PLUS, LONG, SIGNED, CONST_CAST, REM_ASSIGN, XOR_ASSIGN, TYPEID, AND_ASSIGN, CHAR8T, REM, CHAR32T, DYNAMIC_CAST, REINTERPRET_CAST, THIS, COLON_COLON, EQ, GEQ, ASSIGN, ARROW_STAR, UNSIGNED, WCHART, DOUBLE, SHORT, NEQ, SHIFT_RIGHT, DIV, DOT_STAR, DIV_ASSIGN, MINUS_ASSIGN, CARET, FALSE, DOT, EXCLAMATION, DELETE, LPAREN, ARROW, LANGLE, LBRACKET, INTEGER_LITERAL, RANGLE, PIPE, AMP, STRING_LITERAL, QUEST, NEW, TRUE, FLOATING_LITERAL, COMMA, CHAR_LITERAL, NULLPTR, ID, TILDE}" ruleName="ExpressionList"/>
          </sequence>
          <token first="{RPAREN}" name="RPAREN"/>
         </sequence>
        </group>
       </optional>
      </sequence>
      <token first="{COLON}" name="COLON"/>
     </sequence>
     <action first="{ID}">
      <token first="{ID}" name="ID"/>
     </action>
    </sequence>
   </group>
  </rule>
  <rule first="{ID}" name="Nonterminal">
   <action first="{ID}">
    <group first="{ID}">
     <token first="{ID}" name="ID"/>
    </group>
   </action>
  </rule>
  <rule first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" name="Primitive">
   <group first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
    <switch first="{ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}">
     <case first="{EMPTY}">
      <action first="{EMPTY}">
       <token first="{EMPTY}" name="EMPTY"/>
      </action>
     </case>
     <case first="{ANY}">
      <action first="{ANY}">
       <token first="{ANY}" name="ANY"/>
      </action>
     </case>
     <case first="{ID}">
      <action first="{ID}">
       <token first="{ID}" name="ID"/>
      </action>
     </case>
     <case first="{CHAR_LITERAL}">
      <action first="{CHAR_LITERAL}">
       <token first="{CHAR_LITERAL}" name="CHAR_LITERAL"/>
      </action>
     </case>
     <case first="{STRING_LITERAL}">
      <action first="{STRING_LITERAL}">
       <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
      </action>
     </case>
    </switch>
   </group>
  </rule>
  <rule first="{LPAREN}" name="Grouping">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <token first="{LPAREN}" name="LPAREN"/>
       <nonterminal first="{LPAREN, AMP, ANY, STRING_LITERAL, EMPTY, CHAR_LITERAL, ID}" ruleName="Choice"/>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
