<parserFile>
 <grammar name="ParserFileParser">
  <rule first="{#, #, ID, LBRACKET, SEMICOLON}" name="ParserFile">
   <action first="{#, #, ID, LBRACKET, SEMICOLON}">
    <group first="{#, #, ID, LBRACKET, SEMICOLON}">
     <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
      <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
       <sequence first="{#, ID}">
        <action first="{#}">
         <empty first="{#}"/>
        </action>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="ExportModule"/>
        </action>
       </sequence>
       <kleene first="{#, #, ID, LBRACKET, SEMICOLON}">
        <group first="{#, ID, LBRACKET, SEMICOLON}">
         <action first="{#, ID, LBRACKET, SEMICOLON}">
          <nonterminal first="{#, ID, LBRACKET, SEMICOLON}" ruleName="Import"/>
         </action>
        </group>
       </kleene>
      </sequence>
      <kleene first="{#, ID}">
       <group first="{ID}">
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="Parser"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="Parser">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <nonterminal first="{ID}" ruleName="ParserKeyword"/>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, ID, USING, MAIN}">
        <nonterminal first="{ID, USING, MAIN}" ruleName="ParserStatement"/>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID, USING, MAIN}" name="ParserStatement">
   <choice first="{ID, USING, MAIN}">
    <choice first="{ID, USING, MAIN}">
     <choice first="{ID, MAIN}">
      <nonterminal first="{ID}" ruleName="LexerStatement"/>
      <nonterminal first="{MAIN}" ruleName="MainStatement"/>
     </choice>
     <nonterminal first="{USING}" ruleName="UsingStatement"/>
    </choice>
    <nonterminal first="{ID}" ruleName="RuleStatement"/>
   </choice>
  </rule>
  <rule first="{ID}" name="LexerStatement">
   <sequence first="{ID}">
    <sequence first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerKeyword"/>
     <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}" ruleName="TypeId"/>
    </sequence>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{MAIN}" name="MainStatement">
   <sequence first="{MAIN}">
    <token first="{MAIN}" name="MAIN"/>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{USING}" name="UsingStatement">
   <action first="{USING}">
    <group first="{USING}">
     <sequence first="{USING}">
      <sequence first="{USING}">
       <token first="{USING}" name="USING"/>
       <nonterminal first="{ID}" ruleName="QualifiedId"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleStatement">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <sequence first="{ID}">
          <action first="{ID}">
           <token first="{ID}" name="ID"/>
          </action>
          <optional first="{#, LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="ParametersAndVariables"/>
          </optional>
         </sequence>
         <optional first="{#, COLON}">
          <nonterminal first="{COLON}" ruleName="ReturnType"/>
         </optional>
        </sequence>
        <token first="{PRODUCES}" name="PRODUCES"/>
       </sequence>
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="RuleBody"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN}" name="ParametersAndVariables">
   <sequence first="{LPAREN}">
    <sequence first="{LPAREN}">
     <token first="{LPAREN}" name="LPAREN"/>
     <optional first="{#, ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
      <group first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
       <list first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
        <sequence first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
         <action first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
          <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}" ruleName="ParamOrVariable"/>
         </action>
         <kleene first="{#, COMMA}">
          <sequence first="{COMMA}">
           <token first="{COMMA}" name="COMMA"/>
           <action first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
            <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}" ruleName="ParamOrVariable"/>
           </action>
          </sequence>
         </kleene>
        </sequence>
       </list>
      </group>
     </optional>
    </sequence>
    <token first="{RPAREN}" name="RPAREN"/>
   </sequence>
  </rule>
  <rule first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}" name="ParamOrVariable">
   <choice first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, VAR, #, #}">
    <sequence first="{VAR}">
     <sequence first="{VAR}">
      <token first="{VAR}" name="VAR"/>
      <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}" ruleName="TypeId"/>
     </sequence>
     <action first="{ID, COLON_COLON, STAR, OPERATOR, AMP, #, #}">
      <nonterminal first="{ID, COLON_COLON, STAR, OPERATOR, AMP, #, #}" ruleName="Declarator"/>
     </action>
    </sequence>
    <sequence first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}">
     <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}" ruleName="TypeId"/>
     <action first="{ID, COLON_COLON, STAR, OPERATOR, AMP, #, #}">
      <nonterminal first="{ID, COLON_COLON, STAR, OPERATOR, AMP, #, #}" ruleName="Declarator"/>
     </action>
    </sequence>
   </choice>
  </rule>
  <rule first="{COLON}" name="ReturnType">
   <sequence first="{COLON}">
    <token first="{COLON}" name="COLON"/>
    <action first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}">
     <nonterminal first="{ID, UNSIGNED, VOLATILE, COLON_COLON, BOOL, INT, SIGNED, LONG, FLOAT, DOUBLE, WCHART, CONST, CHAR8T, VOID, CHAR16T, CHAR, SHORT, CHAR32T, #, #}" ruleName="TypeId"/>
    </action>
   </sequence>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="RuleBody">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Choice"/>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="Choice">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
      <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Sequence"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
          <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Sequence"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="Sequence">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
      <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Difference"/>
      </action>
      <kleene first="{#, STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
       <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
        <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
         <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Difference"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="Difference">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
      <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="List"/>
      </action>
      <kleene first="{#, MINUS}">
       <group first="{MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
          <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="List"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="List">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
      <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Prefix"/>
      </action>
      <optional first="{#, REM}">
       <group first="{REM}">
        <sequence first="{REM}">
         <token first="{REM}" name="REM"/>
         <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
          <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Prefix"/>
         </action>
        </sequence>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" name="Prefix">
   <choice first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}">
    <sequence first="{AMP}">
     <token first="{AMP}" name="AMP"/>
     <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
      <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}" ruleName="Postfix"/>
     </action>
    </sequence>
    <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
     <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}" ruleName="Postfix"/>
    </action>
   </choice>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}" name="Postfix">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
      <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <choice first="{PLUS, STAR, QUEST}">
         <choice first="{PLUS, STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}" name="Primary">
   <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
    <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
     <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
      <sequence first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
       <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
        <choice first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN}">
         <choice first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}">
          <action first="{ID}">
           <nonterminal first="{ID}" ruleName="RuleCall"/>
          </action>
          <action first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}">
           <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}" ruleName="Primitive"/>
          </action>
         </choice>
         <action first="{LPAREN}">
          <nonterminal first="{LPAREN}" ruleName="Grouping"/>
         </action>
        </choice>
       </group>
       <optional first="{#, EXCLAMATION}">
        <group first="{EXCLAMATION}">
         <action first="{EXCLAMATION}">
          <token first="{EXCLAMATION}" name="EXCLAMATION"/>
         </action>
        </group>
       </optional>
      </sequence>
      <optional first="{#, LBRACE}">
       <group first="{LBRACE}">
        <action first="{LBRACE}">
         <group first="{LBRACE}">
          <sequence first="{LBRACE}">
           <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
           <optional first="{#, DIV}">
            <group first="{DIV}">
             <sequence first="{DIV}">
              <token first="{DIV}" name="DIV"/>
              <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
             </sequence>
            </group>
           </optional>
          </sequence>
         </group>
        </action>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleCall">
   <group first="{ID}">
    <sequence first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <action first="{ID}">
        <nonterminal first="{ID}" ruleName="Nonterminal"/>
       </action>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <sequence first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <token first="{LPAREN}" name="LPAREN"/>
           </action>
           <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, DELETE, FALSE, TRUE, NULLPTR, NEW, INTEGER_LITERAL, FLOATING_LITERAL, TYPEID, UNSIGNED, THIS, VOLATILE, PLUS, MINUS_MINUS, COLON_COLON, SHIFT_RIGHT, STAR, REM, PLUS_ASSIGN, SHIFT_LEFT_ASSIGN, AND_ASSIGN, BOOL, OR_ASSIGN, INT, SIGNED, SHIFT_LEFT, LONG, FLOAT, REM_ASSIGN, DOUBLE, WCHART, MINUS, DIV, ASSIGN, MUL_ASSIGN, GEQ, CONST, PLUS_PLUS, MINUS_ASSIGN, DIV_ASSIGN, XOR_ASSIGN, CHAR8T, VOID, CHAR16T, SIZEOF, OPERATOR, CHAR, DYNAMIC_CAST, LEQ, SHORT, STATIC_CAST, SHIFT_RIGHT_ASSIGN, REINTERPRET_CAST, CONST_CAST, CHAR32T, DISJUNCTION, DOT_STAR, ARROW_STAR, EQ, NEQ, AMP_AMP, DOT, TILDE, EXCLAMATION, CARET, RANGLE, QUEST, PIPE, LBRACKET, COMMA, LPAREN, ARROW, AMP, LANGLE, #, #}" ruleName="ExpressionList"/>
          </sequence>
          <token first="{RPAREN}" name="RPAREN"/>
         </sequence>
        </group>
       </optional>
      </sequence>
      <token first="{COLON}" name="COLON"/>
     </sequence>
     <action first="{ID}">
      <token first="{ID}" name="ID"/>
     </action>
    </sequence>
   </group>
  </rule>
  <rule first="{ID}" name="Nonterminal">
   <action first="{ID}">
    <group first="{ID}">
     <token first="{ID}" name="ID"/>
    </group>
   </action>
  </rule>
  <rule first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}" name="Primitive">
   <group first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}">
    <choice first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY}">
     <choice first="{CHAR_LITERAL, ID, EMPTY, ANY}">
      <choice first="{ID, EMPTY, ANY}">
       <choice first="{EMPTY, ANY}">
        <action first="{EMPTY}">
         <token first="{EMPTY}" name="EMPTY"/>
        </action>
        <action first="{ANY}">
         <token first="{ANY}" name="ANY"/>
        </action>
       </choice>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </choice>
      <action first="{CHAR_LITERAL}">
       <token first="{CHAR_LITERAL}" name="CHAR_LITERAL"/>
      </action>
     </choice>
     <action first="{STRING_LITERAL}">
      <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
     </action>
    </choice>
   </group>
  </rule>
  <rule first="{LPAREN}" name="Grouping">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <token first="{LPAREN}" name="LPAREN"/>
       <nonterminal first="{STRING_LITERAL, CHAR_LITERAL, ID, EMPTY, ANY, LPAREN, AMP}" ruleName="Choice"/>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
