<parserFile>
 <grammar name="ParserFileParser">
  <rule first="{#, SEMICOLON, LBRACKET, ID, #}" name="ParserFile">
   <action first="{#, SEMICOLON, LBRACKET, ID, #}">
    <group first="{#, SEMICOLON, LBRACKET, ID, #}">
     <sequence first="{#, SEMICOLON, LBRACKET, ID, #}">
      <sequence first="{#, SEMICOLON, LBRACKET, ID, #}">
       <sequence first="{#, ID}">
        <action first="{#}">
         <empty first="{#}"/>
        </action>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="ExportModule"/>
        </action>
       </sequence>
       <kleene first="{#, SEMICOLON, LBRACKET, ID, #}">
        <group first="{SEMICOLON, LBRACKET, ID, #}">
         <action first="{SEMICOLON, LBRACKET, ID, #}">
          <nonterminal first="{SEMICOLON, LBRACKET, ID, #}" ruleName="Import"/>
         </action>
        </group>
       </kleene>
      </sequence>
      <kleene first="{#, ID}">
       <group first="{ID}">
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="Parser"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="Parser">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <nonterminal first="{ID}" ruleName="ParserKeyword"/>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, USING, ID, MAIN}">
        <nonterminal first="{USING, ID, MAIN}" ruleName="ParserStatement"/>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{USING, ID, MAIN}" name="ParserStatement">
   <choice first="{USING, ID, MAIN}">
    <choice first="{USING, ID, MAIN}">
     <choice first="{ID, MAIN}">
      <nonterminal first="{ID}" ruleName="LexerStatement"/>
      <nonterminal first="{MAIN}" ruleName="MainStatement"/>
     </choice>
     <nonterminal first="{USING}" ruleName="UsingStatement"/>
    </choice>
    <nonterminal first="{ID}" ruleName="RuleStatement"/>
   </choice>
  </rule>
  <rule first="{ID}" name="LexerStatement">
   <sequence first="{ID}">
    <sequence first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerKeyword"/>
     <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}" ruleName="TypeId"/>
    </sequence>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{MAIN}" name="MainStatement">
   <sequence first="{MAIN}">
    <token first="{MAIN}" name="MAIN"/>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{USING}" name="UsingStatement">
   <action first="{USING}">
    <group first="{USING}">
     <sequence first="{USING}">
      <sequence first="{USING}">
       <token first="{USING}" name="USING"/>
       <nonterminal first="{ID}" ruleName="QualifiedId"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleStatement">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <sequence first="{ID}">
          <action first="{ID}">
           <token first="{ID}" name="ID"/>
          </action>
          <optional first="{#, LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="ParametersAndVariables"/>
          </optional>
         </sequence>
         <optional first="{#, COLON}">
          <nonterminal first="{COLON}" ruleName="ReturnType"/>
         </optional>
        </sequence>
        <token first="{PRODUCES}" name="PRODUCES"/>
       </sequence>
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="RuleBody"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN}" name="ParametersAndVariables">
   <sequence first="{LPAREN}">
    <sequence first="{LPAREN}">
     <token first="{LPAREN}" name="LPAREN"/>
     <optional first="{#, WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
      <group first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
       <list first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
        <sequence first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
         <action first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
          <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}" ruleName="ParamOrVariable"/>
         </action>
         <kleene first="{#, COMMA}">
          <sequence first="{COMMA}">
           <token first="{COMMA}" name="COMMA"/>
           <action first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
            <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}" ruleName="ParamOrVariable"/>
           </action>
          </sequence>
         </kleene>
        </sequence>
       </list>
      </group>
     </optional>
    </sequence>
    <token first="{RPAREN}" name="RPAREN"/>
   </sequence>
  </rule>
  <rule first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}" name="ParamOrVariable">
   <choice first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, VAR, #, #}">
    <sequence first="{VAR}">
     <sequence first="{VAR}">
      <token first="{VAR}" name="VAR"/>
      <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}" ruleName="TypeId"/>
     </sequence>
     <action first="{COLON_COLON, OPERATOR, STAR, AMP, ID, #, #}">
      <nonterminal first="{COLON_COLON, OPERATOR, STAR, AMP, ID, #, #}" ruleName="Declarator"/>
     </action>
    </sequence>
    <sequence first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}">
     <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}" ruleName="TypeId"/>
     <action first="{COLON_COLON, OPERATOR, STAR, AMP, ID, #, #}">
      <nonterminal first="{COLON_COLON, OPERATOR, STAR, AMP, ID, #, #}" ruleName="Declarator"/>
     </action>
    </sequence>
   </choice>
  </rule>
  <rule first="{COLON}" name="ReturnType">
   <sequence first="{COLON}">
    <token first="{COLON}" name="COLON"/>
    <action first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}">
     <nonterminal first="{WCHART, COLON_COLON, INT, SIGNED, SHORT, VOLATILE, FLOAT, VOID, CHAR16T, UNSIGNED, BOOL, LONG, CHAR32T, DOUBLE, CHAR8T, CHAR, CONST, ID, #, #}" ruleName="TypeId"/>
    </action>
   </sequence>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="RuleBody">
   <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Choice"/>
   </action>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Choice">
   <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Sequence"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
          <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Sequence"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Sequence">
   <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Difference"/>
      </action>
      <kleene first="{#, AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <group first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
        <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
         <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Difference"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Difference">
   <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="List"/>
      </action>
      <kleene first="{#, MINUS}">
       <group first="{MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
          <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="List"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="List">
   <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Prefix"/>
      </action>
      <optional first="{#, REM}">
       <group first="{REM}">
        <sequence first="{REM}">
         <token first="{REM}" name="REM"/>
         <action first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
          <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Prefix"/>
         </action>
        </sequence>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Prefix">
   <choice first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <sequence first="{AMP}">
     <token first="{AMP}" name="AMP"/>
     <action first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <nonterminal first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Postfix"/>
     </action>
    </sequence>
    <action first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <nonterminal first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Postfix"/>
    </action>
   </choice>
  </rule>
  <rule first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Postfix">
   <action first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <action first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <nonterminal first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <choice first="{STAR, PLUS, QUEST}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" name="Primary">
   <action first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
    <group first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
     <sequence first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
      <sequence first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
       <group first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
        <choice first="{CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}">
         <choice first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}">
          <action first="{ID}">
           <nonterminal first="{ID}" ruleName="RuleCall"/>
          </action>
          <action first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}">
           <nonterminal first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Primitive"/>
          </action>
         </choice>
         <action first="{LPAREN}">
          <nonterminal first="{LPAREN}" ruleName="Grouping"/>
         </action>
        </choice>
       </group>
       <optional first="{#, EXCLAMATION}">
        <group first="{EXCLAMATION}">
         <action first="{EXCLAMATION}">
          <token first="{EXCLAMATION}" name="EXCLAMATION"/>
         </action>
        </group>
       </optional>
      </sequence>
      <optional first="{#, LBRACE}">
       <group first="{LBRACE}">
        <action first="{LBRACE}">
         <group first="{LBRACE}">
          <sequence first="{LBRACE}">
           <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
           <optional first="{#, DIV}">
            <group first="{DIV}">
             <sequence first="{DIV}">
              <token first="{DIV}" name="DIV"/>
              <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
             </sequence>
            </group>
           </optional>
          </sequence>
         </group>
        </action>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleCall">
   <group first="{ID}">
    <sequence first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <action first="{ID}">
        <nonterminal first="{ID}" ruleName="Nonterminal"/>
       </action>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <sequence first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <token first="{LPAREN}" name="LPAREN"/>
           </action>
           <nonterminal first="{SHIFT_RIGHT, SHIFT_LEFT, LEQ, GEQ, NEQ, ARROW_STAR, DISJUNCTION, AMP_AMP, DOT_STAR, EQ, SIZEOF, CONST_CAST, TRUE, PLUS_ASSIGN, OR_ASSIGN, WCHART, COLON_COLON, INT, SIGNED, DELETE, REM_ASSIGN, ASSIGN, SHORT, VOLATILE, OPERATOR, FLOAT, VOID, MINUS, REM, MUL_ASSIGN, MINUS_ASSIGN, SHIFT_RIGHT_ASSIGN, STAR, XOR_ASSIGN, DIV_ASSIGN, AND_ASSIGN, CHAR16T, UNSIGNED, TYPEID, BOOL, DIV, PLUS, LONG, CHAR32T, DOUBLE, DYNAMIC_CAST, THIS, MINUS_MINUS, CHAR8T, NEW, FALSE, REINTERPRET_CAST, NULLPTR, STATIC_CAST, CHAR, SHIFT_LEFT_ASSIGN, PLUS_PLUS, CONST, AMP, CHAR_LITERAL, LANGLE, RANGLE, INTEGER_LITERAL, DOT, LPAREN, ARROW, LBRACKET, QUEST, ID, STRING_LITERAL, COMMA, TILDE, PIPE, FLOATING_LITERAL, EXCLAMATION, CARET, #, #}" ruleName="ExpressionList"/>
          </sequence>
          <token first="{RPAREN}" name="RPAREN"/>
         </sequence>
        </group>
       </optional>
      </sequence>
      <token first="{COLON}" name="COLON"/>
     </sequence>
     <action first="{ID}">
      <token first="{ID}" name="ID"/>
     </action>
    </sequence>
   </group>
  </rule>
  <rule first="{ID}" name="Nonterminal">
   <action first="{ID}">
    <group first="{ID}">
     <token first="{ID}" name="ID"/>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}" name="Primitive">
   <group first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}">
    <choice first="{CHAR_LITERAL, ID, STRING_LITERAL, EMPTY, ANY}">
     <choice first="{CHAR_LITERAL, ID, EMPTY, ANY}">
      <choice first="{ID, EMPTY, ANY}">
       <choice first="{EMPTY, ANY}">
        <action first="{EMPTY}">
         <token first="{EMPTY}" name="EMPTY"/>
        </action>
        <action first="{ANY}">
         <token first="{ANY}" name="ANY"/>
        </action>
       </choice>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </choice>
      <action first="{CHAR_LITERAL}">
       <token first="{CHAR_LITERAL}" name="CHAR_LITERAL"/>
      </action>
     </choice>
     <action first="{STRING_LITERAL}">
      <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
     </action>
    </choice>
   </group>
  </rule>
  <rule first="{LPAREN}" name="Grouping">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <token first="{LPAREN}" name="LPAREN"/>
       <nonterminal first="{AMP, CHAR_LITERAL, LPAREN, ID, STRING_LITERAL, EMPTY, ANY}" ruleName="Choice"/>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
