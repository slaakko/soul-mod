<parserFile>
 <grammar name="ParserFileParser">
  <rule first="{#, ID, #, SEMICOLON, LBRACKET}" name="ParserFile">
   <action first="{#, ID, #, SEMICOLON, LBRACKET}">
    <group first="{#, ID, #, SEMICOLON, LBRACKET}">
     <sequence first="{#, ID, #, SEMICOLON, LBRACKET}">
      <sequence first="{#, ID, #, SEMICOLON, LBRACKET}">
       <sequence first="{#, ID}">
        <action first="{#}">
         <empty first="{#}"/>
        </action>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="ExportModule"/>
        </action>
       </sequence>
       <kleene first="{#, ID, #, SEMICOLON, LBRACKET}">
        <group first="{ID, #, SEMICOLON, LBRACKET}">
         <action first="{ID, #, SEMICOLON, LBRACKET}">
          <nonterminal first="{ID, #, SEMICOLON, LBRACKET}" ruleName="Import"/>
         </action>
        </group>
       </kleene>
      </sequence>
      <kleene first="{#, ID}">
       <group first="{ID}">
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="Parser"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="Parser">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <nonterminal first="{ID}" ruleName="ParserKeyword"/>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, ID, USING, MAIN}">
        <nonterminal first="{ID, USING, MAIN}" ruleName="ParserStatement"/>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID, USING, MAIN}" name="ParserStatement">
   <choice first="{ID, USING, MAIN}">
    <choice first="{ID, USING, MAIN}">
     <choice first="{ID, MAIN}">
      <nonterminal first="{ID}" ruleName="LexerStatement"/>
      <nonterminal first="{MAIN}" ruleName="MainStatement"/>
     </choice>
     <nonterminal first="{USING}" ruleName="UsingStatement"/>
    </choice>
    <nonterminal first="{ID}" ruleName="RuleStatement"/>
   </choice>
  </rule>
  <rule first="{ID}" name="LexerStatement">
   <sequence first="{ID}">
    <sequence first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerKeyword"/>
     <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}" ruleName="TypeId"/>
    </sequence>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{MAIN}" name="MainStatement">
   <sequence first="{MAIN}">
    <token first="{MAIN}" name="MAIN"/>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{USING}" name="UsingStatement">
   <action first="{USING}">
    <group first="{USING}">
     <sequence first="{USING}">
      <sequence first="{USING}">
       <token first="{USING}" name="USING"/>
       <nonterminal first="{ID}" ruleName="QualifiedId"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleStatement">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <sequence first="{ID}">
          <action first="{ID}">
           <token first="{ID}" name="ID"/>
          </action>
          <optional first="{#, LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="ParametersAndVariables"/>
          </optional>
         </sequence>
         <optional first="{#, COLON}">
          <nonterminal first="{COLON}" ruleName="ReturnType"/>
         </optional>
        </sequence>
        <token first="{PRODUCES}" name="PRODUCES"/>
       </sequence>
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="RuleBody"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN}" name="ParametersAndVariables">
   <sequence first="{LPAREN}">
    <sequence first="{LPAREN}">
     <token first="{LPAREN}" name="LPAREN"/>
     <optional first="{#, #, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
      <group first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
       <list first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
        <sequence first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
         <action first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
          <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}" ruleName="ParamOrVariable"/>
         </action>
         <kleene first="{#, COMMA}">
          <sequence first="{COMMA}">
           <token first="{COMMA}" name="COMMA"/>
           <action first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
            <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}" ruleName="ParamOrVariable"/>
           </action>
          </sequence>
         </kleene>
        </sequence>
       </list>
      </group>
     </optional>
    </sequence>
    <token first="{RPAREN}" name="RPAREN"/>
   </sequence>
  </rule>
  <rule first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}" name="ParamOrVariable">
   <choice first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, VAR}">
    <sequence first="{VAR}">
     <sequence first="{VAR}">
      <token first="{VAR}" name="VAR"/>
      <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}" ruleName="TypeId"/>
     </sequence>
     <action first="{#, #, ID, STAR, COLON_COLON, OPERATOR, AMP}">
      <nonterminal first="{#, #, ID, STAR, COLON_COLON, OPERATOR, AMP}" ruleName="Declarator"/>
     </action>
    </sequence>
    <sequence first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}">
     <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}" ruleName="TypeId"/>
     <action first="{#, #, ID, STAR, COLON_COLON, OPERATOR, AMP}">
      <nonterminal first="{#, #, ID, STAR, COLON_COLON, OPERATOR, AMP}" ruleName="Declarator"/>
     </action>
    </sequence>
   </choice>
  </rule>
  <rule first="{COLON}" name="ReturnType">
   <sequence first="{COLON}">
    <token first="{COLON}" name="COLON"/>
    <action first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}">
     <nonterminal first="{#, #, ID, COLON_COLON, VOID, BOOL, INT, DOUBLE, VOLATILE, SIGNED, LONG, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT}" ruleName="TypeId"/>
    </action>
   </sequence>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="RuleBody">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Choice"/>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="Choice">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
      <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Sequence"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
          <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Sequence"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="Sequence">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
      <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Difference"/>
      </action>
      <kleene first="{#, CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
       <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
        <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
         <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Difference"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="Difference">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
      <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="List"/>
      </action>
      <kleene first="{#, MINUS}">
       <group first="{MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
          <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="List"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="List">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
      <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Prefix"/>
      </action>
      <optional first="{#, REM}">
       <group first="{REM}">
        <sequence first="{REM}">
         <token first="{REM}" name="REM"/>
         <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
          <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Prefix"/>
         </action>
        </sequence>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" name="Prefix">
   <choice first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}">
    <sequence first="{AMP}">
     <token first="{AMP}" name="AMP"/>
     <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
      <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}" ruleName="Postfix"/>
     </action>
    </sequence>
    <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
     <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}" ruleName="Postfix"/>
    </action>
   </choice>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}" name="Postfix">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
      <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <choice first="{STAR, PLUS, QUEST}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}" name="Primary">
   <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
    <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
     <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
      <sequence first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
       <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
        <choice first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN}">
         <choice first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}">
          <action first="{ID}">
           <nonterminal first="{ID}" ruleName="RuleCall"/>
          </action>
          <action first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}">
           <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}" ruleName="Primitive"/>
          </action>
         </choice>
         <action first="{LPAREN}">
          <nonterminal first="{LPAREN}" ruleName="Grouping"/>
         </action>
        </choice>
       </group>
       <optional first="{#, EXCLAMATION}">
        <group first="{EXCLAMATION}">
         <action first="{EXCLAMATION}">
          <token first="{EXCLAMATION}" name="EXCLAMATION"/>
         </action>
        </group>
       </optional>
      </sequence>
      <optional first="{#, LBRACE}">
       <group first="{LBRACE}">
        <action first="{LBRACE}">
         <group first="{LBRACE}">
          <sequence first="{LBRACE}">
           <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
           <optional first="{#, DIV}">
            <group first="{DIV}">
             <sequence first="{DIV}">
              <token first="{DIV}" name="DIV"/>
              <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
             </sequence>
            </group>
           </optional>
          </sequence>
         </group>
        </action>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleCall">
   <group first="{ID}">
    <sequence first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <action first="{ID}">
        <nonterminal first="{ID}" ruleName="Nonterminal"/>
       </action>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <sequence first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <token first="{LPAREN}" name="LPAREN"/>
           </action>
           <nonterminal first="{CHAR_LITERAL, #, #, STRING_LITERAL, FLOATING_LITERAL, INTEGER_LITERAL, ID, DELETE, TRUE, FALSE, NULLPTR, NEW, LEQ, MINUS, PLUS_ASSIGN, SHIFT_RIGHT, DISJUNCTION, ARROW_STAR, NEQ, AMP_AMP, SHIFT_LEFT, DOT_STAR, STAR, DIV, PLUS_PLUS, REM, MINUS_MINUS, COLON_COLON, ASSIGN, MUL_ASSIGN, MINUS_ASSIGN, DIV_ASSIGN, EQ, REM_ASSIGN, GEQ, PLUS, STATIC_CAST, VOID, XOR_ASSIGN, SIZEOF, REINTERPRET_CAST, CONST_CAST, SHIFT_LEFT_ASSIGN, BOOL, INT, DOUBLE, VOLATILE, OR_ASSIGN, SHIFT_RIGHT_ASSIGN, AND_ASSIGN, OPERATOR, SIGNED, LONG, TYPEID, THIS, DYNAMIC_CAST, SHORT, UNSIGNED, CHAR8T, CHAR16T, CHAR, CHAR32T, WCHART, CONST, FLOAT, COMMA, TILDE, LPAREN, LBRACKET, AMP, RANGLE, LANGLE, ARROW, EXCLAMATION, PIPE, QUEST, CARET, DOT}" ruleName="ExpressionList"/>
          </sequence>
          <token first="{RPAREN}" name="RPAREN"/>
         </sequence>
        </group>
       </optional>
      </sequence>
      <token first="{COLON}" name="COLON"/>
     </sequence>
     <action first="{ID}">
      <token first="{ID}" name="ID"/>
     </action>
    </sequence>
   </group>
  </rule>
  <rule first="{ID}" name="Nonterminal">
   <action first="{ID}">
    <group first="{ID}">
     <token first="{ID}" name="ID"/>
    </group>
   </action>
  </rule>
  <rule first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}" name="Primitive">
   <group first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}">
    <choice first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY}">
     <choice first="{CHAR_LITERAL, ID, ANY, EMPTY}">
      <choice first="{ID, ANY, EMPTY}">
       <choice first="{ANY, EMPTY}">
        <action first="{EMPTY}">
         <token first="{EMPTY}" name="EMPTY"/>
        </action>
        <action first="{ANY}">
         <token first="{ANY}" name="ANY"/>
        </action>
       </choice>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </choice>
      <action first="{CHAR_LITERAL}">
       <token first="{CHAR_LITERAL}" name="CHAR_LITERAL"/>
      </action>
     </choice>
     <action first="{STRING_LITERAL}">
      <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
     </action>
    </choice>
   </group>
  </rule>
  <rule first="{LPAREN}" name="Grouping">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <token first="{LPAREN}" name="LPAREN"/>
       <nonterminal first="{CHAR_LITERAL, STRING_LITERAL, ID, ANY, EMPTY, LPAREN, AMP}" ruleName="Choice"/>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
