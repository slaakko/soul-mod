<parserFile>
 <grammar name="LexerFileParser">
  <rule first="{#, #, LBRACKET, LBRACE, RBRACE, SEMICOLON, ID, ACTIONS, RULES, VARIABLES}" name="LexerFile">
   <action first="{#, #, LBRACKET, LBRACE, RBRACE, SEMICOLON, ID, ACTIONS, RULES, VARIABLES}">
    <group first="{#, #, LBRACKET, LBRACE, RBRACE, SEMICOLON, ID, ACTIONS, RULES, VARIABLES}">
     <sequence first="{#, #, LBRACKET, LBRACE, RBRACE, SEMICOLON, ID, ACTIONS, RULES, VARIABLES}">
      <sequence first="{#, #, LBRACKET, LBRACE, SEMICOLON, ID, ACTIONS, RULES, VARIABLES}">
       <sequence first="{#, #, LBRACKET, LBRACE, SEMICOLON, ID}">
        <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
         <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
          <sequence first="{#, #, LBRACKET, SEMICOLON, ID}">
           <sequence first="{#, ID}">
            <action first="{#}">
             <empty first="{#}"/>
            </action>
            <action first="{ID}">
             <nonterminal first="{ID}" ruleName="ExportModule"/>
            </action>
           </sequence>
           <nonterminal first="{#, #, LBRACKET, SEMICOLON, ID}" ruleName="Imports"/>
          </sequence>
          <nonterminal first="{ID}" ruleName="LexerKeyword"/>
         </sequence>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, ACTIONS, RULES, VARIABLES}">
        <group first="{ACTIONS, RULES, VARIABLES}">
         <nonterminal first="{ACTIONS, RULES, VARIABLES}" ruleName="LexerContent"/>
        </group>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, LBRACKET, SEMICOLON, ID}" name="Imports">
   <kleene first="{#, #, LBRACKET, SEMICOLON, ID}">
    <group first="{#, LBRACKET, SEMICOLON, ID}">
     <action first="{#, LBRACKET, SEMICOLON, ID}">
      <nonterminal first="{#, LBRACKET, SEMICOLON, ID}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{ACTIONS, RULES, VARIABLES}" name="LexerContent">
   <group first="{ACTIONS, RULES, VARIABLES}">
    <choice first="{ACTIONS, RULES, VARIABLES}">
     <choice first="{RULES, VARIABLES}">
      <nonterminal first="{RULES}" ruleName="Rules"/>
      <nonterminal first="{VARIABLES}" ruleName="Variables"/>
     </choice>
     <nonterminal first="{ACTIONS}" ruleName="Actions"/>
    </choice>
   </group>
  </rule>
  <rule first="{RULES}" name="Rules">
   <group first="{RULES}">
    <sequence first="{RULES}">
     <sequence first="{RULES}">
      <sequence first="{RULES}">
       <token first="{RULES}" name="RULES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STRING_LITERAL}">
       <group first="{STRING_LITERAL}">
        <action first="{STRING_LITERAL}">
         <nonterminal first="{STRING_LITERAL}" ruleName="Rule"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{STRING_LITERAL}" name="Rule">
   <action first="{STRING_LITERAL}">
    <group first="{STRING_LITERAL}">
     <sequence first="{STRING_LITERAL}">
      <sequence first="{STRING_LITERAL}">
       <action first="{STRING_LITERAL}">
        <nonterminal first="{STRING_LITERAL}" ruleName="ExprString"/>
       </action>
       <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
      </sequence>
      <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{VARIABLES}" name="Variables">
   <group first="{VARIABLES}">
    <sequence first="{VARIABLES}">
     <sequence first="{VARIABLES}">
      <sequence first="{VARIABLES}">
       <token first="{VARIABLES}" name="VARIABLES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
       <group first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
        <action first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
         <nonterminal first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}" ruleName="Variable"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}" name="Variable">
   <action first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
    <group first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
     <sequence first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
      <sequence first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}">
       <nonterminal first="{VOID, BOOL, INT, WCHART, SIGNED, FLOAT, CHAR8T, LONG, CHAR32T, UNSIGNED, DOUBLE, COLON_COLON, CHAR, SHORT, CHAR16T, ID, CONST, VOLATILE, #, #}" ruleName="TypeId"/>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ACTIONS}" name="Actions">
   <group first="{ACTIONS}">
    <sequence first="{ACTIONS}">
     <sequence first="{ACTIONS}">
      <sequence first="{ACTIONS}">
       <token first="{ACTIONS}" name="ACTIONS"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, ASSIGN, LBRACE, DOLLAR}">
       <group first="{#, ASSIGN, LBRACE, DOLLAR}">
        <sequence first="{#, ASSIGN, LBRACE, DOLLAR}">
         <sequence first="{#, ASSIGN, DOLLAR}">
          <action first="{#, DOLLAR}">
           <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
          </action>
          <token first="{ASSIGN}" name="ASSIGN"/>
         </sequence>
         <action first="{LBRACE}">
          <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, DOLLAR}" name="Action">
   <choice first="{#, DOLLAR}">
    <sequence first="{DOLLAR}">
     <sequence first="{DOLLAR}">
      <sequence first="{DOLLAR}">
       <token first="{DOLLAR}" name="DOLLAR"/>
       <token first="{LPAREN}" name="LPAREN"/>
      </sequence>
      <action first="{INTEGER_LITERAL}">
       <token first="{INTEGER_LITERAL}" name="INTEGER_LITERAL"/>
      </action>
     </sequence>
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </sequence>
    <action first="{#}">
     <empty first="{#}"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
