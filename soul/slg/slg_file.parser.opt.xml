<parserFile>
 <grammar name="SlgFileParser">
  <rule first="{PROJECT}" name="SlgFile">
   <action first="{PROJECT}">
    <group first="{PROJECT}">
     <sequence first="{PROJECT}">
      <sequence first="{PROJECT}">
       <sequence first="{PROJECT}">
        <token first="{PROJECT}" name="PROJECT"/>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="QualifiedId"/>
        </action>
       </sequence>
       <token first="{SEMICOLON}" name="SEMICOLON"/>
      </sequence>
      <kleene first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}">
       <group first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}">
        <action first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}">
         <nonterminal first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}" ruleName="SlgFileDeclaration"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}" name="SlgFileDeclaration">
   <choice first="{#, EXTERN, ID, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}">
    <choice first="{#, EXTERN, TOKENS, KEYWORDS, EXPRESSIONS, SEMICOLON, FILEPATH}">
     <choice first="{#, EXTERN, TOKENS, KEYWORDS, SEMICOLON, FILEPATH}">
      <action first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}">
       <nonterminal first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}" ruleName="TokenFileDeclaration"/>
      </action>
      <action first="{KEYWORDS}">
       <nonterminal first="{KEYWORDS}" ruleName="KeywordFileDeclaration"/>
      </action>
     </choice>
     <action first="{EXPRESSIONS}">
      <nonterminal first="{EXPRESSIONS}" ruleName="ExpressionFileDeclaration"/>
     </action>
    </choice>
    <action first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerFileDeclaration"/>
    </action>
   </choice>
  </rule>
  <rule first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}" name="TokenFileDeclaration">
   <action first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}">
    <group first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}">
     <sequence first="{#, EXTERN, TOKENS, SEMICOLON, FILEPATH}">
      <sequence first="{#, EXTERN, TOKENS, FILEPATH}">
       <sequence first="{#, EXTERN, TOKENS}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <token first="{TOKENS}" name="TOKENS"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{KEYWORDS}" name="KeywordFileDeclaration">
   <action first="{KEYWORDS}">
    <group first="{KEYWORDS}">
     <sequence first="{KEYWORDS}">
      <sequence first="{KEYWORDS}">
       <token first="{KEYWORDS}" name="KEYWORDS"/>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{EXPRESSIONS}" name="ExpressionFileDeclaration">
   <action first="{EXPRESSIONS}">
    <group first="{EXPRESSIONS}">
     <sequence first="{EXPRESSIONS}">
      <sequence first="{EXPRESSIONS}">
       <token first="{EXPRESSIONS}" name="EXPRESSIONS"/>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="LexerFileDeclaration">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <nonterminal first="{ID}" ruleName="LexerKeyword"/>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
