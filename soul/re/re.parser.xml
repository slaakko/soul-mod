<parserFile>
 <grammar name="RegExParser">
  <rule first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" name="RegularExpression">
   <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
    <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" name="Alternative">
   <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
    <group first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
     <sequence first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
      <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
       <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
          <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" name="Catenation">
   <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
    <group first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
     <sequence first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
      <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
       <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
       <group first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
        <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
         <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" name="Repetition">
   <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
    <group first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
     <sequence first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
      <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
       <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <choice first="{STAR, PLUS, QUEST}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" name="Primary">
   <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
    <group first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
     <choice first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
      <choice first="{MINUS, CHARACTER, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
       <choice first="{MINUS, CHARACTER, CARET, ESCAPE, DOT, LPAREN}">
        <choice first="{MINUS, CHARACTER, CARET, ESCAPE, LPAREN}">
         <choice first="{MINUS, CARET, ESCAPE, LPAREN}">
          <choice first="{CARET, ESCAPE, LPAREN}">
           <choice first="{ESCAPE, LPAREN}">
            <sequence first="{LPAREN}">
             <sequence first="{LPAREN}">
              <token first="{LPAREN}" name="LPAREN"/>
              <action first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}">
               <nonterminal first="{MINUS, CHARACTER, LBRACE, CARET, LBRACKET, ESCAPE, DOT, LPAREN}" ruleName="Alternative"/>
              </action>
             </sequence>
             <expectation first="{RPAREN}">
              <token first="{RPAREN}" name="RPAREN"/>
             </expectation>
            </sequence>
            <action first="{ESCAPE}">
             <token first="{ESCAPE}" name="ESCAPE"/>
            </action>
           </choice>
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </choice>
          <action first="{MINUS}">
           <token first="{MINUS}" name="MINUS"/>
          </action>
         </choice>
         <action first="{CHARACTER}">
          <token first="{CHARACTER}" name="CHARACTER"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </choice>
      <action first="{LBRACE}">
       <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
        <sequence first="{#, MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
          <group first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
           <action first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
            <nonterminal first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}" name="Range">
   <action first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
    <group first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
     <sequence first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
      <nonterminal first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
          <nonterminal first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}" name="Char">
   <choice first="{MINUS, STAR, PLUS, RBRACE, CHARACTER, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
    <choice first="{MINUS, STAR, PLUS, RBRACE, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
     <choice first="{STAR, PLUS, RBRACE, LBRACE, QUEST, CARET, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
      <choice first="{STAR, PLUS, RBRACE, LBRACE, QUEST, PIPE, LBRACKET, ESCAPE, DOT, RPAREN, LPAREN}">
       <choice first="{STAR, PLUS, RBRACE, LBRACE, QUEST, PIPE, LBRACKET, DOT, RPAREN, LPAREN}">
        <choice first="{STAR, PLUS, RBRACE, LBRACE, QUEST, PIPE, LBRACKET, RPAREN, LPAREN}">
         <choice first="{STAR, PLUS, RBRACE, LBRACE, PIPE, LBRACKET, RPAREN, LPAREN}">
          <choice first="{STAR, RBRACE, LBRACE, PIPE, LBRACKET, RPAREN, LPAREN}">
           <choice first="{RBRACE, LBRACE, PIPE, LBRACKET, RPAREN, LPAREN}">
            <choice first="{RBRACE, LBRACE, LBRACKET, RPAREN, LPAREN}">
             <choice first="{LBRACE, LBRACKET, RPAREN, LPAREN}">
              <choice first="{LBRACKET, RPAREN, LPAREN}">
               <choice first="{RPAREN, LPAREN}">
                <action first="{LPAREN}">
                 <token first="{LPAREN}" name="LPAREN"/>
                </action>
                <action first="{RPAREN}">
                 <token first="{RPAREN}" name="RPAREN"/>
                </action>
               </choice>
               <action first="{LBRACKET}">
                <token first="{LBRACKET}" name="LBRACKET"/>
               </action>
              </choice>
              <action first="{LBRACE}">
               <token first="{LBRACE}" name="LBRACE"/>
              </action>
             </choice>
             <action first="{RBRACE}">
              <token first="{RBRACE}" name="RBRACE"/>
             </action>
            </choice>
            <action first="{PIPE}">
             <token first="{PIPE}" name="PIPE"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{CARET}">
       <token first="{CARET}" name="CARET"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHARACTER}">
     <token first="{CHARACTER}" name="CHARACTER"/>
    </action>
   </choice>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
