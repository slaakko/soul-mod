<parserFile>
 <grammar name="RegExParser">
  <rule first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" name="RegularExpression">
   <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
    <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" name="Alternative">
   <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
    <group first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
     <sequence first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
      <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
          <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" name="Catenation">
   <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
    <group first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
     <sequence first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
      <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
       <group first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
        <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
         <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" name="Repetition">
   <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
    <group first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
     <sequence first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
      <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
       <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Primary"/>
      </action>
      <optional first="{#, QUEST, STAR, PLUS}">
       <group first="{QUEST, STAR, PLUS}">
        <choice first="{QUEST, STAR, PLUS}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" name="Primary">
   <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
    <group first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
     <choice first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
      <choice first="{LPAREN, DOT, CARET, LBRACKET, CHARACTER, MINUS, ESCAPE}">
       <choice first="{LPAREN, DOT, CARET, CHARACTER, MINUS, ESCAPE}">
        <choice first="{LPAREN, CARET, CHARACTER, MINUS, ESCAPE}">
         <choice first="{LPAREN, CARET, MINUS, ESCAPE}">
          <choice first="{LPAREN, CARET, ESCAPE}">
           <choice first="{LPAREN, ESCAPE}">
            <sequence first="{LPAREN}">
             <sequence first="{LPAREN}">
              <token first="{LPAREN}" name="LPAREN"/>
              <action first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}">
               <nonterminal first="{LPAREN, DOT, CARET, LBRACE, LBRACKET, CHARACTER, MINUS, ESCAPE}" ruleName="Alternative"/>
              </action>
             </sequence>
             <expectation first="{RPAREN}">
              <token first="{RPAREN}" name="RPAREN"/>
             </expectation>
            </sequence>
            <action first="{ESCAPE}">
             <token first="{ESCAPE}" name="ESCAPE"/>
            </action>
           </choice>
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </choice>
          <action first="{MINUS}">
           <token first="{MINUS}" name="MINUS"/>
          </action>
         </choice>
         <action first="{CHARACTER}">
          <token first="{CHARACTER}" name="CHARACTER"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </choice>
      <action first="{LBRACE}">
       <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
        <sequence first="{#, QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
          <group first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
           <action first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
            <nonterminal first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}" name="Range">
   <action first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
    <group first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
     <sequence first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
      <nonterminal first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
          <nonterminal first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}" name="Char">
   <choice first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, CHARACTER, MINUS, STAR, PLUS, ESCAPE}">
    <choice first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, MINUS, STAR, PLUS, ESCAPE}">
     <choice first="{QUEST, LPAREN, DOT, PIPE, CARET, LBRACE, LBRACKET, RPAREN, RBRACE, STAR, PLUS, ESCAPE}">
      <choice first="{QUEST, LPAREN, DOT, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE, STAR, PLUS, ESCAPE}">
       <choice first="{QUEST, LPAREN, DOT, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE, STAR, PLUS}">
        <choice first="{QUEST, LPAREN, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE, STAR, PLUS}">
         <choice first="{LPAREN, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE, STAR, PLUS}">
          <choice first="{LPAREN, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE, STAR}">
           <choice first="{LPAREN, PIPE, LBRACE, LBRACKET, RPAREN, RBRACE}">
            <choice first="{LPAREN, LBRACE, LBRACKET, RPAREN, RBRACE}">
             <choice first="{LPAREN, LBRACE, LBRACKET, RPAREN}">
              <choice first="{LPAREN, LBRACKET, RPAREN}">
               <choice first="{LPAREN, RPAREN}">
                <action first="{LPAREN}">
                 <token first="{LPAREN}" name="LPAREN"/>
                </action>
                <action first="{RPAREN}">
                 <token first="{RPAREN}" name="RPAREN"/>
                </action>
               </choice>
               <action first="{LBRACKET}">
                <token first="{LBRACKET}" name="LBRACKET"/>
               </action>
              </choice>
              <action first="{LBRACE}">
               <token first="{LBRACE}" name="LBRACE"/>
              </action>
             </choice>
             <action first="{RBRACE}">
              <token first="{RBRACE}" name="RBRACE"/>
             </action>
            </choice>
            <action first="{PIPE}">
             <token first="{PIPE}" name="PIPE"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{CARET}">
       <token first="{CARET}" name="CARET"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHARACTER}">
     <token first="{CHARACTER}" name="CHARACTER"/>
    </action>
   </choice>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
