<parserFile>
 <grammar name="RegExParser">
  <rule first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" name="RegularExpression">
   <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
    <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" name="Alternative">
   <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
    <group first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
     <sequence first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
      <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
       <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
          <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" name="Catenation">
   <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
    <group first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
     <sequence first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
      <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
       <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
       <group first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
        <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
         <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" name="Repetition">
   <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
    <group first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
     <sequence first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
      <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
       <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <choice first="{PLUS, STAR, QUEST}">
         <choice first="{PLUS, STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" name="Primary">
   <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
    <group first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
     <choice first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
      <choice first="{MINUS, CARET, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
       <choice first="{MINUS, CARET, CHARACTER, DOT, LPAREN, ESCAPE}">
        <choice first="{MINUS, CARET, CHARACTER, LPAREN, ESCAPE}">
         <choice first="{MINUS, CARET, LPAREN, ESCAPE}">
          <choice first="{CARET, LPAREN, ESCAPE}">
           <choice first="{LPAREN, ESCAPE}">
            <sequence first="{LPAREN}">
             <sequence first="{LPAREN}">
              <token first="{LPAREN}" name="LPAREN"/>
              <action first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}">
               <nonterminal first="{MINUS, CARET, LBRACE, CHARACTER, DOT, LPAREN, LBRACKET, ESCAPE}" ruleName="Alternative"/>
              </action>
             </sequence>
             <expectation first="{RPAREN}">
              <token first="{RPAREN}" name="RPAREN"/>
             </expectation>
            </sequence>
            <action first="{ESCAPE}">
             <token first="{ESCAPE}" name="ESCAPE"/>
            </action>
           </choice>
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </choice>
          <action first="{MINUS}">
           <token first="{MINUS}" name="MINUS"/>
          </action>
         </choice>
         <action first="{CHARACTER}">
          <token first="{CHARACTER}" name="CHARACTER"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </choice>
      <action first="{LBRACE}">
       <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
        <sequence first="{#, PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
          <group first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
           <action first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
            <nonterminal first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}" name="Range">
   <action first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
    <group first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
     <sequence first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
      <nonterminal first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
          <nonterminal first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}" name="Char">
   <choice first="{PLUS, MINUS, STAR, CARET, LBRACE, CHARACTER, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
    <choice first="{PLUS, MINUS, STAR, CARET, LBRACE, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
     <choice first="{PLUS, STAR, CARET, LBRACE, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
      <choice first="{PLUS, STAR, LBRACE, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE, ESCAPE}">
       <choice first="{PLUS, STAR, LBRACE, QUEST, DOT, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE}">
        <choice first="{PLUS, STAR, LBRACE, QUEST, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE}">
         <choice first="{PLUS, STAR, LBRACE, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE}">
          <choice first="{STAR, LBRACE, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE}">
           <choice first="{LBRACE, LPAREN, RPAREN, RBRACE, LBRACKET, PIPE}">
            <choice first="{LBRACE, LPAREN, RPAREN, RBRACE, LBRACKET}">
             <choice first="{LBRACE, LPAREN, RPAREN, LBRACKET}">
              <choice first="{LPAREN, RPAREN, LBRACKET}">
               <choice first="{LPAREN, RPAREN}">
                <action first="{LPAREN}">
                 <token first="{LPAREN}" name="LPAREN"/>
                </action>
                <action first="{RPAREN}">
                 <token first="{RPAREN}" name="RPAREN"/>
                </action>
               </choice>
               <action first="{LBRACKET}">
                <token first="{LBRACKET}" name="LBRACKET"/>
               </action>
              </choice>
              <action first="{LBRACE}">
               <token first="{LBRACE}" name="LBRACE"/>
              </action>
             </choice>
             <action first="{RBRACE}">
              <token first="{RBRACE}" name="RBRACE"/>
             </action>
            </choice>
            <action first="{PIPE}">
             <token first="{PIPE}" name="PIPE"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{CARET}">
       <token first="{CARET}" name="CARET"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHARACTER}">
     <token first="{CHARACTER}" name="CHARACTER"/>
    </action>
   </choice>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
