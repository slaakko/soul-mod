<parserFile>
 <grammar name="RegExParser">
  <rule first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" name="RegularExpression">
   <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
    <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" name="Alternative">
   <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
    <group first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
     <sequence first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
      <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
       <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
          <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" name="Catenation">
   <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
    <group first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
     <sequence first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
      <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
       <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
       <group first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
        <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
         <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" name="Repetition">
   <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
    <group first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
     <sequence first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
      <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
       <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <switch first="{STAR, PLUS, QUEST}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" name="Primary">
   <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
    <group first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
     <switch first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}">
          <nonterminal first="{MINUS, DOT, LPAREN, LBRACKET, LBRACE, CHARACTER, CARET, ESCAPE}" ruleName="Alternative"/>
         </action>
        </sequence>
        <expectation first="{RPAREN}">
         <token first="{RPAREN}" name="RPAREN"/>
        </expectation>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{CARET}">
       <action first="{CARET}">
        <token first="{CARET}" name="CARET"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHARACTER}">
       <action first="{CHARACTER}">
        <token first="{CHARACTER}" name="CHARACTER"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
      <case first="{LBRACE}">
       <action first="{LBRACE}">
        <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
        <sequence first="{#, MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
          <group first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
           <action first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
            <nonterminal first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}" name="Range">
   <action first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
    <group first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
     <sequence first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
      <nonterminal first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
          <nonterminal first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}" name="Char">
   <switch first="{MINUS, STAR, PLUS, DOT, LPAREN, RPAREN, LBRACKET, LBRACE, RBRACE, CHARACTER, QUEST, PIPE, CARET, ESCAPE}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{LBRACE}">
     <action first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
     </action>
    </case>
    <case first="{RBRACE}">
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </case>
    <case first="{PIPE}">
     <action first="{PIPE}">
      <token first="{PIPE}" name="PIPE"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{CARET}">
     <action first="{CARET}">
      <token first="{CARET}" name="CARET"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHARACTER}">
     <action first="{CHARACTER}">
      <token first="{CHARACTER}" name="CHARACTER"/>
     </action>
    </case>
   </switch>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
