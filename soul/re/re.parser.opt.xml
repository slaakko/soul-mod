<parserFile>
 <grammar name="RegExParser">
  <rule first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" name="RegularExpression">
   <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
    <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" name="Alternative">
   <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
    <group first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
     <sequence first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
      <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
       <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
          <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" name="Catenation">
   <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
    <group first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
     <sequence first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
      <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
       <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
       <group first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
        <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
         <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" name="Repetition">
   <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
    <group first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
     <sequence first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
      <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
       <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Primary"/>
      </action>
      <optional first="{#, QUEST, PLUS, STAR}">
       <group first="{QUEST, PLUS, STAR}">
        <switch first="{QUEST, PLUS, STAR}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" name="Primary">
   <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
    <group first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
     <switch first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}">
          <nonterminal first="{LBRACKET, LPAREN, LBRACE, DOT, CARET, CHARACTER, ESCAPE, MINUS}" ruleName="Alternative"/>
         </action>
        </sequence>
        <expectation first="{RPAREN}">
         <token first="{RPAREN}" name="RPAREN"/>
        </expectation>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{CARET}">
       <action first="{CARET}">
        <token first="{CARET}" name="CARET"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHARACTER}">
       <action first="{CHARACTER}">
        <token first="{CHARACTER}" name="CHARACTER"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
      <case first="{LBRACE}">
       <action first="{LBRACE}">
        <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
        <sequence first="{#, RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
          <group first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
           <action first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
            <nonterminal first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}" name="Range">
   <action first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
    <group first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
     <sequence first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
      <nonterminal first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
          <nonterminal first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}" name="Char">
   <switch first="{RBRACE, QUEST, LBRACKET, LPAREN, RPAREN, LBRACE, DOT, CARET, PIPE, CHARACTER, ESCAPE, PLUS, MINUS, STAR}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{LBRACE}">
     <action first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
     </action>
    </case>
    <case first="{RBRACE}">
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </case>
    <case first="{PIPE}">
     <action first="{PIPE}">
      <token first="{PIPE}" name="PIPE"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{CARET}">
     <action first="{CARET}">
      <token first="{CARET}" name="CARET"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHARACTER}">
     <action first="{CHARACTER}">
      <token first="{CHARACTER}" name="CHARACTER"/>
     </action>
    </case>
   </switch>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
