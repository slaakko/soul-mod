<parserFile>
 <grammar name="ParserFileParser">
  <rule first="{#, #, SEMICOLON, ID, LBRACKET}" name="ParserFile">
   <action first="{#, #, SEMICOLON, ID, LBRACKET}">
    <group first="{#, #, SEMICOLON, ID, LBRACKET}">
     <sequence first="{#, #, SEMICOLON, ID, LBRACKET}">
      <sequence first="{#, #, SEMICOLON, ID, LBRACKET}">
       <sequence first="{#, ID}">
        <action first="{#}">
         <empty first="{#}"/>
        </action>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="ExportModule"/>
        </action>
       </sequence>
       <kleene first="{#, #, SEMICOLON, ID, LBRACKET}">
        <group first="{#, SEMICOLON, ID, LBRACKET}">
         <action first="{#, SEMICOLON, ID, LBRACKET}">
          <nonterminal first="{#, SEMICOLON, ID, LBRACKET}" ruleName="Import"/>
         </action>
        </group>
       </kleene>
      </sequence>
      <kleene first="{#, ID}">
       <group first="{ID}">
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="Parser"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="Parser">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <nonterminal first="{ID}" ruleName="ParserKeyword"/>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, USING, ID, MAIN}">
        <nonterminal first="{USING, ID, MAIN}" ruleName="ParserStatement"/>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{USING, ID, MAIN}" name="ParserStatement">
   <choice first="{USING, ID, MAIN}">
    <choice first="{USING, ID, MAIN}">
     <choice first="{ID, MAIN}">
      <nonterminal first="{ID}" ruleName="LexerStatement"/>
      <nonterminal first="{MAIN}" ruleName="MainStatement"/>
     </choice>
     <nonterminal first="{USING}" ruleName="UsingStatement"/>
    </choice>
    <nonterminal first="{ID}" ruleName="RuleStatement"/>
   </choice>
  </rule>
  <rule first="{ID}" name="LexerStatement">
   <sequence first="{ID}">
    <sequence first="{ID}">
     <nonterminal first="{ID}" ruleName="LexerKeyword"/>
     <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}" ruleName="TypeId"/>
    </sequence>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{MAIN}" name="MainStatement">
   <sequence first="{MAIN}">
    <token first="{MAIN}" name="MAIN"/>
    <action first="{SEMICOLON}">
     <token first="{SEMICOLON}" name="SEMICOLON"/>
    </action>
   </sequence>
  </rule>
  <rule first="{USING}" name="UsingStatement">
   <action first="{USING}">
    <group first="{USING}">
     <sequence first="{USING}">
      <sequence first="{USING}">
       <token first="{USING}" name="USING"/>
       <nonterminal first="{ID}" ruleName="QualifiedId"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleStatement">
   <action first="{ID}">
    <group first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <sequence first="{ID}">
        <sequence first="{ID}">
         <sequence first="{ID}">
          <action first="{ID}">
           <token first="{ID}" name="ID"/>
          </action>
          <optional first="{#, LPAREN}">
           <nonterminal first="{LPAREN}" ruleName="ParametersAndVariables"/>
          </optional>
         </sequence>
         <optional first="{#, COLON}">
          <nonterminal first="{COLON}" ruleName="ReturnType"/>
         </optional>
        </sequence>
        <token first="{PRODUCES}" name="PRODUCES"/>
       </sequence>
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="RuleBody"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN}" name="ParametersAndVariables">
   <sequence first="{LPAREN}">
    <sequence first="{LPAREN}">
     <token first="{LPAREN}" name="LPAREN"/>
     <optional first="{#, CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
      <group first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
       <list first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
        <sequence first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
         <action first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
          <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}" ruleName="ParamOrVariable"/>
         </action>
         <kleene first="{#, COMMA}">
          <sequence first="{COMMA}">
           <token first="{COMMA}" name="COMMA"/>
           <action first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
            <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}" ruleName="ParamOrVariable"/>
           </action>
          </sequence>
         </kleene>
        </sequence>
       </list>
      </group>
     </optional>
    </sequence>
    <token first="{RPAREN}" name="RPAREN"/>
   </sequence>
  </rule>
  <rule first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}" name="ParamOrVariable">
   <choice first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, VAR, #, #}">
    <sequence first="{VAR}">
     <sequence first="{VAR}">
      <token first="{VAR}" name="VAR"/>
      <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}" ruleName="TypeId"/>
     </sequence>
     <action first="{OPERATOR, ID, COLON_COLON, STAR, #, AMP, #}">
      <nonterminal first="{OPERATOR, ID, COLON_COLON, STAR, #, AMP, #}" ruleName="Declarator"/>
     </action>
    </sequence>
    <sequence first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}">
     <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}" ruleName="TypeId"/>
     <action first="{OPERATOR, ID, COLON_COLON, STAR, #, AMP, #}">
      <nonterminal first="{OPERATOR, ID, COLON_COLON, STAR, #, AMP, #}" ruleName="Declarator"/>
     </action>
    </sequence>
   </choice>
  </rule>
  <rule first="{COLON}" name="ReturnType">
   <sequence first="{COLON}">
    <token first="{COLON}" name="COLON"/>
    <action first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}">
     <nonterminal first="{CHAR, BOOL, FLOAT, CHAR16T, CONST, DOUBLE, CHAR32T, CHAR8T, WCHART, SIGNED, INT, ID, UNSIGNED, LONG, SHORT, VOID, VOLATILE, COLON_COLON, #, #}" ruleName="TypeId"/>
    </action>
   </sequence>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="RuleBody">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Choice"/>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="Choice">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
      <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Sequence"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
          <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Sequence"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="Sequence">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
      <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Difference"/>
      </action>
      <kleene first="{#, LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
       <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
        <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
         <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Difference"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="Difference">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
      <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="List"/>
      </action>
      <kleene first="{#, MINUS}">
       <group first="{MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
          <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="List"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="List">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
      <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Prefix"/>
      </action>
      <optional first="{#, REM}">
       <group first="{REM}">
        <sequence first="{REM}">
         <token first="{REM}" name="REM"/>
         <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
          <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Prefix"/>
         </action>
        </sequence>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" name="Prefix">
   <choice first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}">
    <sequence first="{AMP}">
     <token first="{AMP}" name="AMP"/>
     <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
      <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" ruleName="Postfix"/>
     </action>
    </sequence>
    <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
     <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" ruleName="Postfix"/>
    </action>
   </choice>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" name="Postfix">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
      <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" ruleName="Primary"/>
      </action>
      <optional first="{#, PLUS, STAR, QUEST}">
       <group first="{PLUS, STAR, QUEST}">
        <choice first="{PLUS, STAR, QUEST}">
         <choice first="{PLUS, STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" name="Primary">
   <action first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
    <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
     <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
      <sequence first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
       <group first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
        <choice first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
         <choice first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
          <action first="{ID}">
           <nonterminal first="{ID}" ruleName="RuleCall"/>
          </action>
          <action first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
           <nonterminal first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" ruleName="Primitive"/>
          </action>
         </choice>
         <action first="{LPAREN}">
          <nonterminal first="{LPAREN}" ruleName="Grouping"/>
         </action>
        </choice>
       </group>
       <optional first="{#, EXCLAMATION}">
        <group first="{EXCLAMATION}">
         <action first="{EXCLAMATION}">
          <token first="{EXCLAMATION}" name="EXCLAMATION"/>
         </action>
        </group>
       </optional>
      </sequence>
      <optional first="{#, LBRACE}">
       <group first="{LBRACE}">
        <action first="{LBRACE}">
         <group first="{LBRACE}">
          <sequence first="{LBRACE}">
           <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
           <optional first="{#, DIV}">
            <group first="{DIV}">
             <sequence first="{DIV}">
              <token first="{DIV}" name="DIV"/>
              <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
             </sequence>
            </group>
           </optional>
          </sequence>
         </group>
        </action>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ID}" name="RuleCall">
   <group first="{ID}">
    <sequence first="{ID}">
     <sequence first="{ID}">
      <sequence first="{ID}">
       <action first="{ID}">
        <nonterminal first="{ID}" ruleName="Nonterminal"/>
       </action>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <sequence first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <token first="{LPAREN}" name="LPAREN"/>
           </action>
           <nonterminal first="{RANGLE, CHAR, THIS, BOOL, SIZEOF, FLOAT, CHAR16T, CONST, NEW, CONST_CAST, DOUBLE, OPERATOR, CHAR32T, REINTERPRET_CAST, TRUE, STATIC_CAST, CHAR8T, DYNAMIC_CAST, WCHART, DOT, LPAREN, LANGLE, SIGNED, INT, ID, UNSIGNED, FLOATING_LITERAL, LONG, INTEGER_LITERAL, SHORT, DELETE, STRING_LITERAL, VOID, CHAR_LITERAL, LBRACKET, VOLATILE, TYPEID, FALSE, NULLPTR, SHIFT_LEFT, MUL_ASSIGN, PLUS_PLUS, PLUS, NEQ, EQ, REM, DIV_ASSIGN, DIV, PLUS_ASSIGN, SHIFT_LEFT_ASSIGN, AND_ASSIGN, OR_ASSIGN, SHIFT_RIGHT, MINUS_ASSIGN, MINUS_MINUS, MINUS, GEQ, ASSIGN, COLON_COLON, REM_ASSIGN, XOR_ASSIGN, DISJUNCTION, LEQ, STAR, ARROW_STAR, AMP_AMP, SHIFT_RIGHT_ASSIGN, DOT_STAR, #, PIPE, COMMA, CARET, TILDE, EXCLAMATION, QUEST, AMP, ARROW, #}" ruleName="ExpressionList"/>
          </sequence>
          <token first="{RPAREN}" name="RPAREN"/>
         </sequence>
        </group>
       </optional>
      </sequence>
      <token first="{COLON}" name="COLON"/>
     </sequence>
     <action first="{ID}">
      <token first="{ID}" name="ID"/>
     </action>
    </sequence>
   </group>
  </rule>
  <rule first="{ID}" name="Nonterminal">
   <action first="{ID}">
    <group first="{ID}">
     <token first="{ID}" name="ID"/>
    </group>
   </action>
  </rule>
  <rule first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}" name="Primitive">
   <group first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
    <choice first="{ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, ANY}">
     <choice first="{ID, CHAR_LITERAL, EMPTY, ANY}">
      <choice first="{ID, EMPTY, ANY}">
       <choice first="{EMPTY, ANY}">
        <action first="{EMPTY}">
         <token first="{EMPTY}" name="EMPTY"/>
        </action>
        <action first="{ANY}">
         <token first="{ANY}" name="ANY"/>
        </action>
       </choice>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </choice>
      <action first="{CHAR_LITERAL}">
       <token first="{CHAR_LITERAL}" name="CHAR_LITERAL"/>
      </action>
     </choice>
     <action first="{STRING_LITERAL}">
      <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
     </action>
    </choice>
   </group>
  </rule>
  <rule first="{LPAREN}" name="Grouping">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <token first="{LPAREN}" name="LPAREN"/>
       <nonterminal first="{LPAREN, ID, STRING_LITERAL, CHAR_LITERAL, EMPTY, AMP, ANY}" ruleName="Choice"/>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
