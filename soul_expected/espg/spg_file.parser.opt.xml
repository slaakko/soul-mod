<parserFile>
 <grammar name="SpgFileParser">
  <rule first="{PROJECT}" name="SpgFile">
   <action first="{PROJECT}">
    <group first="{PROJECT}">
     <sequence first="{PROJECT}">
      <sequence first="{PROJECT}">
       <sequence first="{PROJECT}">
        <token first="{PROJECT}" name="PROJECT"/>
        <action first="{ID}">
         <nonterminal first="{ID}" ruleName="QualifiedId"/>
        </action>
       </sequence>
       <token first="{SEMICOLON}" name="SEMICOLON"/>
      </sequence>
      <kleene first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}">
       <group first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}">
        <action first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}">
         <nonterminal first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}" ruleName="SpgFileDeclaration"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}" name="SpgFileDeclaration">
   <choice first="{#, TOKENS, FILEPATH, EXTERN, ID, SEMICOLON}">
    <action first="{#, FILEPATH, EXTERN, ID, SEMICOLON}">
     <nonterminal first="{#, FILEPATH, EXTERN, ID, SEMICOLON}" ruleName="ParserFileDeclaration"/>
    </action>
    <action first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}">
     <nonterminal first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}" ruleName="TokenFileDeclaration"/>
    </action>
   </choice>
  </rule>
  <rule first="{#, FILEPATH, EXTERN, ID, SEMICOLON}" name="ParserFileDeclaration">
   <action first="{#, FILEPATH, EXTERN, ID, SEMICOLON}">
    <group first="{#, FILEPATH, EXTERN, ID, SEMICOLON}">
     <sequence first="{#, FILEPATH, EXTERN, ID, SEMICOLON}">
      <sequence first="{#, FILEPATH, EXTERN, ID}">
       <sequence first="{#, EXTERN, ID}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <nonterminal first="{ID}" ruleName="ParserKeyword"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}" name="TokenFileDeclaration">
   <action first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}">
    <group first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}">
     <sequence first="{#, TOKENS, FILEPATH, EXTERN, SEMICOLON}">
      <sequence first="{#, TOKENS, FILEPATH, EXTERN}">
       <sequence first="{#, TOKENS, EXTERN}">
        <group first="{#, EXTERN}">
         <choice first="{#, EXTERN}">
          <action first="{EXTERN}">
           <token first="{EXTERN}" name="EXTERN"/>
          </action>
          <action first="{#}">
           <empty first="{#}"/>
          </action>
         </choice>
        </group>
        <token first="{TOKENS}" name="TOKENS"/>
       </sequence>
       <nonterminal first="{FILEPATH}" ruleName="FilePath"/>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
