<parserFile>
 <grammar name="LexerFileParser">
  <rule first="{#, #, VARIABLES, RULES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}" name="LexerFile">
   <action first="{#, #, VARIABLES, RULES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
    <group first="{#, #, VARIABLES, RULES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
     <sequence first="{#, #, VARIABLES, RULES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
      <sequence first="{#, #, VARIABLES, RULES, ACTIONS, ID, LBRACE, LBRACKET, SEMICOLON}">
       <sequence first="{#, #, ID, LBRACE, LBRACKET, SEMICOLON}">
        <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
         <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
          <sequence first="{#, #, ID, LBRACKET, SEMICOLON}">
           <sequence first="{#, ID}">
            <action first="{#}">
             <empty first="{#}"/>
            </action>
            <action first="{ID}">
             <nonterminal first="{ID}" ruleName="ExportModule"/>
            </action>
           </sequence>
           <nonterminal first="{#, #, ID, LBRACKET, SEMICOLON}" ruleName="Imports"/>
          </sequence>
          <nonterminal first="{ID}" ruleName="LexerKeyword"/>
         </sequence>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, VARIABLES, RULES, ACTIONS}">
        <group first="{VARIABLES, RULES, ACTIONS}">
         <nonterminal first="{VARIABLES, RULES, ACTIONS}" ruleName="LexerContent"/>
        </group>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, ID, LBRACKET, SEMICOLON}" name="Imports">
   <kleene first="{#, #, ID, LBRACKET, SEMICOLON}">
    <group first="{#, ID, LBRACKET, SEMICOLON}">
     <action first="{#, ID, LBRACKET, SEMICOLON}">
      <nonterminal first="{#, ID, LBRACKET, SEMICOLON}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{VARIABLES, RULES, ACTIONS}" name="LexerContent">
   <group first="{VARIABLES, RULES, ACTIONS}">
    <switch first="{VARIABLES, RULES, ACTIONS}">
     <case first="{RULES}">
      <nonterminal first="{RULES}" ruleName="Rules"/>
     </case>
     <case first="{VARIABLES}">
      <nonterminal first="{VARIABLES}" ruleName="Variables"/>
     </case>
     <case first="{ACTIONS}">
      <nonterminal first="{ACTIONS}" ruleName="Actions"/>
     </case>
    </switch>
   </group>
  </rule>
  <rule first="{RULES}" name="Rules">
   <group first="{RULES}">
    <sequence first="{RULES}">
     <sequence first="{RULES}">
      <sequence first="{RULES}">
       <token first="{RULES}" name="RULES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STRING_LITERAL}">
       <group first="{STRING_LITERAL}">
        <action first="{STRING_LITERAL}">
         <nonterminal first="{STRING_LITERAL}" ruleName="Rule"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{STRING_LITERAL}" name="Rule">
   <action first="{STRING_LITERAL}">
    <group first="{STRING_LITERAL}">
     <sequence first="{STRING_LITERAL}">
      <sequence first="{STRING_LITERAL}">
       <action first="{STRING_LITERAL}">
        <nonterminal first="{STRING_LITERAL}" ruleName="ExprString"/>
       </action>
       <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
      </sequence>
      <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{VARIABLES}" name="Variables">
   <group first="{VARIABLES}">
    <sequence first="{VARIABLES}">
     <sequence first="{VARIABLES}">
      <sequence first="{VARIABLES}">
       <token first="{VARIABLES}" name="VARIABLES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, #, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
       <group first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
        <action first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
         <nonterminal first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}" ruleName="Variable"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}" name="Variable">
   <action first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
    <group first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
     <sequence first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
      <sequence first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}">
       <nonterminal first="{#, #, CHAR16T, INT, SIGNED, UNSIGNED, SHORT, COLON_COLON, VOID, CONST, BOOL, LONG, CHAR, CHAR32T, WCHART, FLOAT, DOUBLE, VOLATILE, CHAR8T, ID}" ruleName="TypeId"/>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ACTIONS}" name="Actions">
   <group first="{ACTIONS}">
    <sequence first="{ACTIONS}">
     <sequence first="{ACTIONS}">
      <sequence first="{ACTIONS}">
       <token first="{ACTIONS}" name="ACTIONS"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, ASSIGN, LBRACE, DOLLAR}">
       <group first="{#, ASSIGN, LBRACE, DOLLAR}">
        <sequence first="{#, ASSIGN, LBRACE, DOLLAR}">
         <sequence first="{#, ASSIGN, DOLLAR}">
          <action first="{#, DOLLAR}">
           <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
          </action>
          <token first="{ASSIGN}" name="ASSIGN"/>
         </sequence>
         <action first="{LBRACE}">
          <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, DOLLAR}" name="Action">
   <choice first="{#, DOLLAR}">
    <sequence first="{DOLLAR}">
     <sequence first="{DOLLAR}">
      <sequence first="{DOLLAR}">
       <token first="{DOLLAR}" name="DOLLAR"/>
       <token first="{LPAREN}" name="LPAREN"/>
      </sequence>
      <action first="{INTEGER_LITERAL}">
       <token first="{INTEGER_LITERAL}" name="INTEGER_LITERAL"/>
      </action>
     </sequence>
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </sequence>
    <action first="{#}">
     <empty first="{#}"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
