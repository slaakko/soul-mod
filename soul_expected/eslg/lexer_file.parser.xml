<parserFile>
 <grammar name="LexerFileParser">
  <rule first="{#, RULES, VARIABLES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}" name="LexerFile">
   <action first="{#, RULES, VARIABLES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
    <group first="{#, RULES, VARIABLES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
     <sequence first="{#, RULES, VARIABLES, ACTIONS, ID, LBRACE, RBRACE, LBRACKET, SEMICOLON}">
      <sequence first="{#, RULES, VARIABLES, ACTIONS, ID, LBRACE, LBRACKET, SEMICOLON}">
       <sequence first="{#, ID, LBRACE, LBRACKET, SEMICOLON}">
        <sequence first="{#, ID, LBRACKET, SEMICOLON}">
         <sequence first="{#, ID, LBRACKET, SEMICOLON}">
          <sequence first="{#, ID, LBRACKET, SEMICOLON}">
           <sequence first="{#, ID}">
            <action first="{#}">
             <empty first="{#}"/>
            </action>
            <action first="{ID}">
             <nonterminal first="{ID}" ruleName="ExportModule"/>
            </action>
           </sequence>
           <nonterminal first="{#, ID, LBRACKET, SEMICOLON}" ruleName="Imports"/>
          </sequence>
          <nonterminal first="{ID}" ruleName="LexerKeyword"/>
         </sequence>
         <action first="{ID}">
          <token first="{ID}" name="ID"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <kleene first="{#, RULES, VARIABLES, ACTIONS}">
        <group first="{RULES, VARIABLES, ACTIONS}">
         <nonterminal first="{RULES, VARIABLES, ACTIONS}" ruleName="LexerContent"/>
        </group>
       </kleene>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, ID, LBRACKET, SEMICOLON}" name="Imports">
   <kleene first="{#, ID, LBRACKET, SEMICOLON}">
    <group first="{#, ID, LBRACKET, SEMICOLON}">
     <action first="{#, ID, LBRACKET, SEMICOLON}">
      <nonterminal first="{#, ID, LBRACKET, SEMICOLON}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{RULES, VARIABLES, ACTIONS}" name="LexerContent">
   <group first="{RULES, VARIABLES, ACTIONS}">
    <choice first="{RULES, VARIABLES, ACTIONS}">
     <choice first="{RULES, VARIABLES}">
      <nonterminal first="{RULES}" ruleName="Rules"/>
      <nonterminal first="{VARIABLES}" ruleName="Variables"/>
     </choice>
     <nonterminal first="{ACTIONS}" ruleName="Actions"/>
    </choice>
   </group>
  </rule>
  <rule first="{RULES}" name="Rules">
   <group first="{RULES}">
    <sequence first="{RULES}">
     <sequence first="{RULES}">
      <sequence first="{RULES}">
       <token first="{RULES}" name="RULES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STRING_LITERAL}">
       <group first="{STRING_LITERAL}">
        <action first="{STRING_LITERAL}">
         <nonterminal first="{STRING_LITERAL}" ruleName="Rule"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{STRING_LITERAL}" name="Rule">
   <action first="{STRING_LITERAL}">
    <group first="{STRING_LITERAL}">
     <sequence first="{STRING_LITERAL}">
      <sequence first="{STRING_LITERAL}">
       <action first="{STRING_LITERAL}">
        <nonterminal first="{STRING_LITERAL}" ruleName="ExprString"/>
       </action>
       <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
      </sequence>
      <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{VARIABLES}" name="Variables">
   <group first="{VARIABLES}">
    <sequence first="{VARIABLES}">
     <sequence first="{VARIABLES}">
      <sequence first="{VARIABLES}">
       <token first="{VARIABLES}" name="VARIABLES"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
       <group first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
        <action first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
         <nonterminal first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}" ruleName="Variable"/>
        </action>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}" name="Variable">
   <action first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
    <group first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
     <sequence first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, SEMICOLON, AMP}">
      <sequence first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, AMP}">
       <nonterminal first="{#, STAR, COLON_COLON, CHAR, CHAR8T, CHAR16T, CHAR32T, WCHART, BOOL, SHORT, INT, LONG, SIGNED, UNSIGNED, FLOAT, DOUBLE, VOID, CONST, VOLATILE, ID, LPAREN, LBRACKET, LANGLE, AMP}" ruleName="TypeId"/>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{SEMICOLON}" name="SEMICOLON"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{ACTIONS}" name="Actions">
   <group first="{ACTIONS}">
    <sequence first="{ACTIONS}">
     <sequence first="{ACTIONS}">
      <sequence first="{ACTIONS}">
       <token first="{ACTIONS}" name="ACTIONS"/>
       <token first="{LBRACE}" name="LBRACE"/>
      </sequence>
      <kleene first="{#, ASSIGN, LBRACE, DOLLAR}">
       <group first="{#, ASSIGN, LBRACE, DOLLAR}">
        <sequence first="{#, ASSIGN, LBRACE, DOLLAR}">
         <sequence first="{#, ASSIGN, DOLLAR}">
          <action first="{#, DOLLAR}">
           <nonterminal first="{#, DOLLAR}" ruleName="Action"/>
          </action>
          <token first="{ASSIGN}" name="ASSIGN"/>
         </sequence>
         <action first="{LBRACE}">
          <nonterminal first="{LBRACE}" ruleName="CompoundStatement"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
     <token first="{RBRACE}" name="RBRACE"/>
    </sequence>
   </group>
  </rule>
  <rule first="{#, DOLLAR}" name="Action">
   <choice first="{#, DOLLAR}">
    <sequence first="{DOLLAR}">
     <sequence first="{DOLLAR}">
      <sequence first="{DOLLAR}">
       <token first="{DOLLAR}" name="DOLLAR"/>
       <token first="{LPAREN}" name="LPAREN"/>
      </sequence>
      <action first="{INTEGER_LITERAL}">
       <token first="{INTEGER_LITERAL}" name="INTEGER_LITERAL"/>
      </action>
     </sequence>
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </sequence>
    <action first="{#}">
     <empty first="{#}"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
