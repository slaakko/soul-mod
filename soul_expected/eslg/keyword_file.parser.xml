<parserFile>
 <grammar name="KeywordFileParser">
  <rule first="{#, #, SEMICOLON, ID, RBRACE, LBRACE, LBRACKET, LPAREN, KEYWORDS}" name="KeywordFile">
   <action first="{#, #, SEMICOLON, ID, RBRACE, LBRACE, LBRACKET, LPAREN, KEYWORDS}">
    <group first="{#, #, SEMICOLON, ID, RBRACE, LBRACE, LBRACKET, LPAREN, KEYWORDS}">
     <sequence first="{#, #, SEMICOLON, ID, RBRACE, LBRACE, LBRACKET, LPAREN, KEYWORDS}">
      <sequence first="{#, #, SEMICOLON, ID, LBRACE, LBRACKET, LPAREN, KEYWORDS}">
       <sequence first="{#, #, SEMICOLON, ID, LBRACE, LBRACKET, KEYWORDS}">
        <sequence first="{#, #, SEMICOLON, ID, LBRACKET, KEYWORDS}">
         <sequence first="{#, #, SEMICOLON, ID, LBRACKET, KEYWORDS}">
          <sequence first="{#, #, SEMICOLON, ID, LBRACKET}">
           <action first="{#}">
            <empty first="{#}"/>
           </action>
           <nonterminal first="{#, #, SEMICOLON, ID, LBRACKET}" ruleName="Imports"/>
          </sequence>
          <token first="{KEYWORDS}" name="KEYWORDS"/>
         </sequence>
         <action first="{ID}">
          <nonterminal first="{ID}" ruleName="QualifiedId"/>
         </action>
        </sequence>
        <token first="{LBRACE}" name="LBRACE"/>
       </sequence>
       <optional first="{#, LPAREN}">
        <group first="{LPAREN}">
         <list first="{LPAREN}">
          <sequence first="{LPAREN}">
           <action first="{LPAREN}">
            <nonterminal first="{LPAREN}" ruleName="Keyword"/>
           </action>
           <kleene first="{#, COMMA}">
            <sequence first="{COMMA}">
             <token first="{COMMA}" name="COMMA"/>
             <action first="{LPAREN}">
              <nonterminal first="{LPAREN}" ruleName="Keyword"/>
             </action>
            </sequence>
           </kleene>
          </sequence>
         </list>
        </group>
       </optional>
      </sequence>
      <token first="{RBRACE}" name="RBRACE"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{#, #, SEMICOLON, ID, LBRACKET}" name="Imports">
   <kleene first="{#, #, SEMICOLON, ID, LBRACKET}">
    <group first="{#, SEMICOLON, ID, LBRACKET}">
     <action first="{#, SEMICOLON, ID, LBRACKET}">
      <nonterminal first="{#, SEMICOLON, ID, LBRACKET}" ruleName="Import"/>
     </action>
    </group>
   </kleene>
  </rule>
  <rule first="{LPAREN}" name="Keyword">
   <action first="{LPAREN}">
    <group first="{LPAREN}">
     <sequence first="{LPAREN}">
      <sequence first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{STRING_LITERAL}">
          <token first="{STRING_LITERAL}" name="STRING_LITERAL"/>
         </action>
        </sequence>
        <token first="{COMMA}" name="COMMA"/>
       </sequence>
       <action first="{ID}">
        <token first="{ID}" name="ID"/>
       </action>
      </sequence>
      <token first="{RPAREN}" name="RPAREN"/>
     </sequence>
    </group>
   </action>
  </rule>
 </grammar>
</parserFile>
