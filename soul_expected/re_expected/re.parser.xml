<parserFile>
 <grammar name="RegExParser">
  <rule first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="RegularExpression">
   <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Alternative">
   <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <group first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
     <sequence first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
      <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
       <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
          <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Catenation">
   <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <group first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
     <sequence first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
      <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
       <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
       <group first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
        <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
         <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Repetition">
   <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <group first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
     <sequence first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
      <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
       <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <choice first="{STAR, PLUS, QUEST}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Primary">
   <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <group first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
     <choice first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
      <choice first="{MINUS, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
       <choice first="{MINUS, LPAREN, DOT, CARET, ESCAPE, CHARACTER}">
        <choice first="{MINUS, LPAREN, CARET, ESCAPE, CHARACTER}">
         <choice first="{MINUS, LPAREN, CARET, ESCAPE}">
          <choice first="{LPAREN, CARET, ESCAPE}">
           <choice first="{LPAREN, ESCAPE}">
            <sequence first="{LPAREN}">
             <sequence first="{LPAREN}">
              <token first="{LPAREN}" name="LPAREN"/>
              <action first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
               <nonterminal first="{MINUS, LBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Alternative"/>
              </action>
             </sequence>
             <token first="{RPAREN}" name="RPAREN"/>
            </sequence>
            <action first="{ESCAPE}">
             <token first="{ESCAPE}" name="ESCAPE"/>
            </action>
           </choice>
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </choice>
          <action first="{MINUS}">
           <token first="{MINUS}" name="MINUS"/>
          </action>
         </choice>
         <action first="{CHARACTER}">
          <token first="{CHARACTER}" name="CHARACTER"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </choice>
      <action first="{LBRACE}">
       <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
        <sequence first="{#, MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
          <group first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
           <action first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
            <nonterminal first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Range">
   <action first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <group first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
     <sequence first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
      <nonterminal first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
          <nonterminal first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}" name="Char">
   <choice first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE, CHARACTER}">
    <choice first="{MINUS, STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE}">
     <choice first="{STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, CARET, LBRACKET, ESCAPE}">
      <choice first="{STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, LBRACKET, ESCAPE}">
       <choice first="{STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, DOT, LBRACKET}">
        <choice first="{STAR, PLUS, RPAREN, QUEST, PIPE, LBRACE, RBRACE, LPAREN, LBRACKET}">
         <choice first="{STAR, PLUS, RPAREN, PIPE, LBRACE, RBRACE, LPAREN, LBRACKET}">
          <choice first="{STAR, RPAREN, PIPE, LBRACE, RBRACE, LPAREN, LBRACKET}">
           <choice first="{RPAREN, PIPE, LBRACE, RBRACE, LPAREN, LBRACKET}">
            <choice first="{RPAREN, LBRACE, RBRACE, LPAREN, LBRACKET}">
             <choice first="{RPAREN, LBRACE, LPAREN, LBRACKET}">
              <choice first="{RPAREN, LPAREN, LBRACKET}">
               <choice first="{RPAREN, LPAREN}">
                <action first="{LPAREN}">
                 <token first="{LPAREN}" name="LPAREN"/>
                </action>
                <action first="{RPAREN}">
                 <token first="{RPAREN}" name="RPAREN"/>
                </action>
               </choice>
               <action first="{LBRACKET}">
                <token first="{LBRACKET}" name="LBRACKET"/>
               </action>
              </choice>
              <action first="{LBRACE}">
               <token first="{LBRACE}" name="LBRACE"/>
              </action>
             </choice>
             <action first="{RBRACE}">
              <token first="{RBRACE}" name="RBRACE"/>
             </action>
            </choice>
            <action first="{PIPE}">
             <token first="{PIPE}" name="PIPE"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{CARET}">
       <token first="{CARET}" name="CARET"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHARACTER}">
     <token first="{CHARACTER}" name="CHARACTER"/>
    </action>
   </choice>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
