<parserFile>
 <grammar name="RegExParser">
  <rule first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" name="RegularExpression">
   <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
    <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Alternative"/>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" name="Alternative">
   <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
    <group first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
      <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, PIPE}">
       <group first="{PIPE}">
        <sequence first="{PIPE}">
         <token first="{PIPE}" name="PIPE"/>
         <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
          <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" name="Catenation">
   <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
    <group first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
      <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
       <group first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
        <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
         <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" name="Repetition">
   <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
    <group first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
     <sequence first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
      <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
       <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, PLUS, QUEST}">
       <group first="{STAR, PLUS, QUEST}">
        <switch first="{STAR, PLUS, QUEST}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" name="Primary">
   <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
    <group first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
     <switch first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}">
          <nonterminal first="{MINUS, DOT, LBRACE, CARET, CHARACTER, ESCAPE, LBRACKET, LPAREN}" ruleName="Alternative"/>
         </action>
        </sequence>
        <token first="{RPAREN}" name="RPAREN"/>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{CARET}">
       <action first="{CARET}">
        <token first="{CARET}" name="CARET"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHARACTER}">
       <action first="{CHARACTER}">
        <token first="{CHARACTER}" name="CHARACTER"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
      <case first="{LBRACE}">
       <action first="{LBRACE}">
        <nonterminal first="{LBRACE}" ruleName="ExpressionReference"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
        <sequence first="{#, STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
         <optional first="{#, CARET}">
          <group first="{CARET}">
           <action first="{CARET}">
            <token first="{CARET}" name="CARET"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
          <group first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
           <action first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
            <nonterminal first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <token first="{RBRACKET}" name="RBRACKET"/>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}" name="Range">
   <action first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
    <group first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
     <sequence first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
      <nonterminal first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
          <nonterminal first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}" name="Char">
   <switch first="{STAR, MINUS, PLUS, DOT, LBRACE, CARET, RPAREN, RBRACE, QUEST, CHARACTER, ESCAPE, LBRACKET, LPAREN, PIPE}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{LBRACE}">
     <action first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
     </action>
    </case>
    <case first="{RBRACE}">
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </case>
    <case first="{PIPE}">
     <action first="{PIPE}">
      <token first="{PIPE}" name="PIPE"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{CARET}">
     <action first="{CARET}">
      <token first="{CARET}" name="CARET"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHARACTER}">
     <action first="{CHARACTER}">
      <token first="{CHARACTER}" name="CHARACTER"/>
     </action>
    </case>
   </switch>
  </rule>
  <rule first="{LBRACE}" name="ExpressionReference">
   <group first="{LBRACE}">
    <sequence first="{LBRACE}">
     <sequence first="{LBRACE}">
      <token first="{LBRACE}" name="LBRACE"/>
      <positive first="{CHARACTER}">
       <group first="{CHARACTER}">
        <action first="{CHARACTER}">
         <token first="{CHARACTER}" name="CHARACTER"/>
        </action>
       </group>
      </positive>
     </sequence>
     <action first="{RBRACE}">
      <token first="{RBRACE}" name="RBRACE"/>
     </action>
    </sequence>
   </group>
  </rule>
 </grammar>
</parserFile>
