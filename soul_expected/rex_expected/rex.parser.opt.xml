<parserFile>
 <grammar name="RexParser">
  <rule first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" name="RegularExpression">
   <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
    <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
     <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" name="Alternative">
   <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
    <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
     <sequence first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
      <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
       <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
          <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" name="Catenation">
   <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
    <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
     <sequence first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
      <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
       <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
       <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
        <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
         <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" name="Repetition">
   <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
    <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
     <sequence first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
      <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
       <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Primary"/>
      </action>
      <optional first="{#, STAR, QUEST, PLUS}">
       <group first="{STAR, QUEST, PLUS}">
        <switch first="{STAR, QUEST, PLUS}">
         <case first="{STAR}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
         </case>
         <case first="{PLUS}">
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </case>
         <case first="{QUEST}">
          <action first="{QUEST}">
           <token first="{QUEST}" name="QUEST"/>
          </action>
         </case>
        </switch>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" name="Primary">
   <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
    <group first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
     <switch first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
      <case first="{LPAREN}">
       <sequence first="{LPAREN}">
        <sequence first="{LPAREN}">
         <token first="{LPAREN}" name="LPAREN"/>
         <action first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}">
          <nonterminal first="{DOT, MINUS, LBRACKET, INVERSE, LPAREN, ESCAPE, CHAR}" ruleName="Alternative"/>
         </action>
        </sequence>
        <token first="{RPAREN}" name="RPAREN"/>
       </sequence>
      </case>
      <case first="{ESCAPE}">
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </case>
      <case first="{INVERSE}">
       <action first="{INVERSE}">
        <token first="{INVERSE}" name="INVERSE"/>
       </action>
      </case>
      <case first="{MINUS}">
       <action first="{MINUS}">
        <token first="{MINUS}" name="MINUS"/>
       </action>
      </case>
      <case first="{CHAR}">
       <action first="{CHAR}">
        <token first="{CHAR}" name="CHAR"/>
       </action>
      </case>
      <case first="{DOT}">
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </case>
      <case first="{LBRACKET}">
       <action first="{LBRACKET}">
        <nonterminal first="{LBRACKET}" ruleName="Class"/>
       </action>
      </case>
     </switch>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
        <sequence first="{#, DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
          <group first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
           <action first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
            <nonterminal first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}" name="Range">
   <action first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
    <group first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
     <sequence first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
      <nonterminal first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
          <nonterminal first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}" name="Char">
   <switch first="{DOT, MINUS, LBRACKET, ALT, STAR, QUEST, INVERSE, PLUS, LPAREN, ESCAPE, CHAR, RPAREN}">
    <case first="{LPAREN}">
     <action first="{LPAREN}">
      <token first="{LPAREN}" name="LPAREN"/>
     </action>
    </case>
    <case first="{RPAREN}">
     <action first="{RPAREN}">
      <token first="{RPAREN}" name="RPAREN"/>
     </action>
    </case>
    <case first="{LBRACKET}">
     <action first="{LBRACKET}">
      <token first="{LBRACKET}" name="LBRACKET"/>
     </action>
    </case>
    <case first="{ALT}">
     <action first="{ALT}">
      <token first="{ALT}" name="ALT"/>
     </action>
    </case>
    <case first="{STAR}">
     <action first="{STAR}">
      <token first="{STAR}" name="STAR"/>
     </action>
    </case>
    <case first="{PLUS}">
     <action first="{PLUS}">
      <token first="{PLUS}" name="PLUS"/>
     </action>
    </case>
    <case first="{QUEST}">
     <action first="{QUEST}">
      <token first="{QUEST}" name="QUEST"/>
     </action>
    </case>
    <case first="{DOT}">
     <action first="{DOT}">
      <token first="{DOT}" name="DOT"/>
     </action>
    </case>
    <case first="{ESCAPE}">
     <action first="{ESCAPE}">
      <token first="{ESCAPE}" name="ESCAPE"/>
     </action>
    </case>
    <case first="{INVERSE}">
     <action first="{INVERSE}">
      <token first="{INVERSE}" name="INVERSE"/>
     </action>
    </case>
    <case first="{MINUS}">
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </case>
    <case first="{CHAR}">
     <action first="{CHAR}">
      <token first="{CHAR}" name="CHAR"/>
     </action>
    </case>
   </switch>
  </rule>
 </grammar>
</parserFile>
