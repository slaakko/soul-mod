<parserFile>
 <grammar name="RexParser">
  <rule first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" name="RegularExpression">
   <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
    <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
     <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Alternative"/>
    </group>
   </action>
  </rule>
  <rule first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" name="Alternative">
   <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
    <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
      <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Catenation"/>
      </action>
      <kleene first="{#, ALT}">
       <group first="{ALT}">
        <sequence first="{ALT}">
         <token first="{ALT}" name="ALT"/>
         <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
          <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Catenation"/>
         </action>
        </sequence>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" name="Catenation">
   <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
    <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
      <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Repetition"/>
      </action>
      <kleene first="{#, DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
       <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
        <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
         <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Repetition"/>
        </action>
       </group>
      </kleene>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" name="Repetition">
   <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
    <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
     <sequence first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
      <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
       <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Primary"/>
      </action>
      <optional first="{#, QUEST, STAR, PLUS}">
       <group first="{QUEST, STAR, PLUS}">
        <choice first="{QUEST, STAR, PLUS}">
         <choice first="{STAR, PLUS}">
          <action first="{STAR}">
           <token first="{STAR}" name="STAR"/>
          </action>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
       </group>
      </optional>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" name="Primary">
   <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
    <group first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
     <choice first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
      <choice first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LPAREN}">
       <choice first="{ESCAPE, CHAR, MINUS, INVERSE, LPAREN}">
        <choice first="{ESCAPE, MINUS, INVERSE, LPAREN}">
         <choice first="{ESCAPE, INVERSE, LPAREN}">
          <choice first="{ESCAPE, LPAREN}">
           <sequence first="{LPAREN}">
            <sequence first="{LPAREN}">
             <token first="{LPAREN}" name="LPAREN"/>
             <action first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}">
              <nonterminal first="{DOT, ESCAPE, CHAR, MINUS, INVERSE, LBRACKET, LPAREN}" ruleName="Alternative"/>
             </action>
            </sequence>
            <token first="{RPAREN}" name="RPAREN"/>
           </sequence>
           <action first="{ESCAPE}">
            <token first="{ESCAPE}" name="ESCAPE"/>
           </action>
          </choice>
          <action first="{INVERSE}">
           <token first="{INVERSE}" name="INVERSE"/>
          </action>
         </choice>
         <action first="{MINUS}">
          <token first="{MINUS}" name="MINUS"/>
         </action>
        </choice>
        <action first="{CHAR}">
         <token first="{CHAR}" name="CHAR"/>
        </action>
       </choice>
       <action first="{DOT}">
        <token first="{DOT}" name="DOT"/>
       </action>
      </choice>
      <action first="{LBRACKET}">
       <nonterminal first="{LBRACKET}" ruleName="Class"/>
      </action>
     </choice>
    </group>
   </action>
  </rule>
  <rule first="{LBRACKET}" name="Class">
   <action first="{LBRACKET}">
    <group first="{LBRACKET}">
     <sequence first="{LBRACKET}">
      <sequence first="{LBRACKET}">
       <action first="{LBRACKET}">
        <token first="{LBRACKET}" name="LBRACKET"/>
       </action>
       <group first="{#, QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
        <sequence first="{#, QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
         <optional first="{#, INVERSE}">
          <group first="{INVERSE}">
           <action first="{INVERSE}">
            <token first="{INVERSE}" name="INVERSE"/>
           </action>
          </group>
         </optional>
         <kleene first="{#, QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
          <group first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
           <action first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
            <nonterminal first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}" ruleName="Range"/>
           </action>
          </group>
         </kleene>
        </sequence>
       </group>
      </sequence>
      <expectation first="{RBRACKET}">
       <token first="{RBRACKET}" name="RBRACKET"/>
      </expectation>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}" name="Range">
   <action first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
    <group first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
     <sequence first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
      <nonterminal first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}" ruleName="Char"/>
      <group first="{#, MINUS}">
       <choice first="{#, MINUS}">
        <sequence first="{MINUS}">
         <token first="{MINUS}" name="MINUS"/>
         <action first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
          <nonterminal first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}" ruleName="Char"/>
         </action>
        </sequence>
        <action first="{#}">
         <empty first="{#}"/>
        </action>
       </choice>
      </group>
     </sequence>
    </group>
   </action>
  </rule>
  <rule first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}" name="Char">
   <choice first="{QUEST, DOT, ESCAPE, CHAR, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
    <choice first="{QUEST, DOT, ESCAPE, MINUS, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
     <choice first="{QUEST, DOT, ESCAPE, INVERSE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
      <choice first="{QUEST, DOT, ESCAPE, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
       <choice first="{QUEST, DOT, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
        <choice first="{QUEST, RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
         <choice first="{RPAREN, LBRACKET, STAR, LPAREN, ALT, PLUS}">
          <choice first="{RPAREN, LBRACKET, STAR, LPAREN, ALT}">
           <choice first="{RPAREN, LBRACKET, LPAREN, ALT}">
            <choice first="{RPAREN, LBRACKET, LPAREN}">
             <choice first="{RPAREN, LPAREN}">
              <action first="{LPAREN}">
               <token first="{LPAREN}" name="LPAREN"/>
              </action>
              <action first="{RPAREN}">
               <token first="{RPAREN}" name="RPAREN"/>
              </action>
             </choice>
             <action first="{LBRACKET}">
              <token first="{LBRACKET}" name="LBRACKET"/>
             </action>
            </choice>
            <action first="{ALT}">
             <token first="{ALT}" name="ALT"/>
            </action>
           </choice>
           <action first="{STAR}">
            <token first="{STAR}" name="STAR"/>
           </action>
          </choice>
          <action first="{PLUS}">
           <token first="{PLUS}" name="PLUS"/>
          </action>
         </choice>
         <action first="{QUEST}">
          <token first="{QUEST}" name="QUEST"/>
         </action>
        </choice>
        <action first="{DOT}">
         <token first="{DOT}" name="DOT"/>
        </action>
       </choice>
       <action first="{ESCAPE}">
        <token first="{ESCAPE}" name="ESCAPE"/>
       </action>
      </choice>
      <action first="{INVERSE}">
       <token first="{INVERSE}" name="INVERSE"/>
      </action>
     </choice>
     <action first="{MINUS}">
      <token first="{MINUS}" name="MINUS"/>
     </action>
    </choice>
    <action first="{CHAR}">
     <token first="{CHAR}" name="CHAR"/>
    </action>
   </choice>
  </rule>
 </grammar>
</parserFile>
